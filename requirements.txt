# Claude Workflow Engine - Requirements
# ============================================================================

# ============================================================================
# LANGGRAPH ORCHESTRATION (3-Level Flow Engine)
# ============================================================================
langgraph>=0.2.0,<1.0.0     # StateGraph orchestration for parallel + conditional execution
langchain-core>=0.3.0,<1.0.0  # Core primitives (TypedDict, Annotated)

# ============================================================================
# GITHUB AUTOMATION & GIT OPERATIONS
# ============================================================================
PyGithub==2.1.1             # GitHub API wrapper for issue/PR management
GitPython==3.1.40           # Git operations

# ============================================================================
# CODE GRAPH ANALYSIS (Dependency Graph Complexity)
# ============================================================================
networkx>=3.1               # Graph construction + algorithms (centrality, pagerank, density)
lizard>=1.17.0              # Multi-language cyclomatic complexity (Python/Java/JS/Go/C++)

# ============================================================================
# LLM & AI INTEGRATION
# ============================================================================
anthropic==0.50.0           # Anthropic SDK for Claude API
requests>=2.31.0            # HTTP requests for Ollama API

# ============================================================================
# LANGGRAPH CHECKPOINTING
# ============================================================================
langgraph-checkpoint-sqlite>=1.0.0  # SQLite-backed state persistence

# ============================================================================
# SYSTEM & UTILITIES
# ============================================================================
python-dotenv==1.0.1        # Environment variable management
psutil>=5.9.8               # System monitoring
loguru>=0.7.0               # Structured logging
rich>=13.0.0                # Beautiful terminal output (progress bars, colors, tables)
toons>=0.5.3                # TOON schema support
watchdog>=3.0.0             # File system watcher (standards hot-reload)

# ============================================================================
# VOICE / TTS (Text-to-Speech)
# ============================================================================
# TTS removed from core requirements: gruut==2.2.3 (transitive dep of TTS>=0.22.0)
# requires networkx>=2.5.0,<3.0.0 which conflicts with networkx>=3.1 (call graph).
# Install separately when voice notifications are needed:
#   pip install -r requirements-optional.txt

# ============================================================================
# DEVELOPMENT & TESTING
# ============================================================================
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
coverage>=7.0.0

# Code Quality
pylint>=2.15.0
flake8>=6.0.0
black>=23.0.0
isort>=5.11.0
mypy>=1.0.0
pre-commit>=3.6.0           # Git hook framework (runs ruff, black, isort on commit)

# Security
bandit>=1.7.5
safety>=2.3.5

# ============================================================================
# MCP SERVER FRAMEWORK
# ============================================================================
mcp>=1.0.0                  # FastMCP server framework (stdio JSON-RPC)
pydantic>=2.0               # Data validation (used by FastMCP)

# ============================================================================
# ASYNC FILE I/O
# ============================================================================
aiofiles>=23.0              # Async file operations
