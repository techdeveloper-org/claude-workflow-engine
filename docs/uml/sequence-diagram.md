# Sequence Diagram

> Auto-generated by Claude Workflow Engine | Last updated: 2026-03-18

```mermaid
sequenceDiagram
    participant 3_level_flow as 3-level-flow
    participant strftime
    participant upper
    participant datetime_now as datetime.now
    participant uuid_uuid4 as uuid.uuid4
    participant strip
    participant str
    participant AtomicJsonStore
    participant Path_cwd as Path.cwd
    participant os_environ_get as os.environ.get
    participant Path
    participant sys_stdin_isatty as sys.stdin.isatty
    participant sys_stdin_read as sys.stdin.read
    participant json_loads as json.loads
    participant event_get as event.get
    participant orchestrator
    participant checkpointer
    participant HybridInferenceManager
    participant RuntimeError
    participant print
    participant list
    participant initial_state_get as initial_state.get
    participant result_get as result.get
    participant len
    participant initial_state_keys as initial_state.keys
    participant memory_dir_exists as memory_dir.exists
    participant policies_dir_exists as policies_dir.exists
    participant Path_home as Path.home
    participant threading_Event as threading.Event
    participant threading_Thread as threading.Thread
    participant engine_thread_start as engine_thread.start
    participant _timeout_event_wait as _timeout_event.wait
    participant flow_trace_converter
    participant sys_exit as sys.exit
    participant arg_startswith as arg.startswith
    participant JsonlAppender
    participant _timeout_event_set as _timeout_event.set
    participant traceback_print_exc as traceback.print_exc
    participant arg_split as arg.split
    participant auto_fix_enforcer as auto-fix-enforcer
    participant msvcrt_locking as msvcrt.locking
    participant f_fileno as f.fileno
    participant f_seek as f.seek
    participant FLAG_DIR_glob as FLAG_DIR.glob
    participant timedelta
    participant MCPResponse
    participant flag_file_unlink as flag_file.unlink
    participant flag_file_stat as flag_file.stat
    participant datetime_fromtimestamp as datetime.fromtimestamp
    participant path_exists as path.exists
    participant total_seconds
    participant path_unlink as path.unlink
    participant path_stat as path.stat
    participant path_read_text as path.read_text
    participant datetime_fromisoformat as datetime.fromisoformat
    participant AutoFixEnforcer
    participant check_fn
    participant int
    participant getattr
    participant result_stdout_strip as result.stdout.strip
    participant critical_files_items as critical_files.items
    participant optional_files_items as optional_files.items
    participant full_path_exists as full_path.exists
    participant state_file_exists as state_file.exists
    participant open
    participant state_get as state.get
    participant project_session
    participant current_session_file_exists as current_session_file.exists
    participant data_get as data.get
    participant required_checks_items as required_checks.items
    participant join
    participant pid_dir_exists as pid_dir.exists
    participant pid_file_exists as pid_file.exists
    participant pid_file_read_text as pid_file.read_text
    participant checker_script_exists as checker_script.exists
    participant split
    participant self_memory_path_rglob as self.memory_path.rglob
    participant line_strip as line.strip
    participant state_file_parent_mkdir as state_file.parent.mkdir
    participant isoformat
    participant json_dump as json.dump
    participant failure_get as failure.get
    participant enumerate
    participant hasattr
    participant sum
    participant policy_tracking_helper
    participant argparse_ArgumentParser as argparse.ArgumentParser
    participant parser_add_argument as parser.add_argument
    participant parser_parse_args as parser.parse_args
    participant json_dumps as json.dumps
    participant PolicyTrackingIntegrator
    participant self_script_path_read_text as self.script_path.read_text
    participant self_content_split as self.content.split
    participant re_match as re.match
    participant lines_insert as lines.insert
    participant self_TRACKING_IMPORT_strip as self.TRACKING_IMPORT.strip
    participant re_search as re.search
    participant match_end as match.end
    participant startswith
    participant self_TRACKING_SETUP_strip as self.TRACKING_SETUP.strip
    participant range
    participant self_script_path_write_text as self.script_path.write_text
    participant self_script_path_with_suffix as self.script_path.with_suffix
    participant backup_path_write_text as backup_path.write_text
    participant auto_integrate_policy_tracking as auto-integrate-policy-tracking
    participant batches_get as batches.get
    participant script_path_exists as script_path.exists
    participant auto_build_validator
    participant os_getcwd as os.getcwd
    participant subprocess_run as subprocess.run
    participant pkg_get as pkg.get
    participant project_info_get as project_info.get
    participant error_output_strip as error_output.strip
    participant root_rglob as root.rglob
    participant os_path_isabs as os.path.isabs
    participant full_path_endswith as full_path.endswith
    participant os_path_exists as os.path.exists
    participant lstrip
    participant result_stderr_strip as result.stderr.strip
    participant py_file_replace as py_file.replace
    participant r_get as r.get
    participant clear_session_handler as clear-session-handler
    participant get_window_state_file
    participant register_window
    participant os_getpid as os.getpid
    participant CLEAR_LOG_parent_mkdir as CLEAR_LOG.parent.mkdir
    participant f_write as f.write
    participant history_file_exists as history_file.exists
    participant raw_strip as raw.strip
    participant content_splitlines as content.splitlines
    participant isinstance
    participant msg_get as msg.get
    participant m_get as m.get
    participant HOOK_STATE_FILE_exists as HOOK_STATE_FILE.exists
    participant HOOK_STATE_FILE_parent_mkdir as HOOK_STATE_FILE.parent.mkdir
    participant LazyClient
    participant CURRENT_SESSION_FILE_exists as CURRENT_SESSION_FILE.exists
    participant session_file_exists as session_file.exists
    participant CURRENT_SESSION_FILE_unlink as CURRENT_SESSION_FILE.unlink
    participant SESSION_START_VOICE_FLAG_write_text as SESSION_START_VOICE_FLAG.write_text
    participant flow_trace_exists as flow_trace.exists
    participant user_input_get as user_input.get
    participant final_decision_get as final_decision.get
    participant replace
    participant handoff_file_write_text as handoff_file.write_text
    participant meta_get as meta.get
    participant SessionIDGenerator
    participant sess_script_exists as sess_script.exists
    participant result_stdout_splitlines as result.stdout.splitlines
    participant sys_path_insert as sys.path.insert
    participant line_startswith as line.startswith
    participant resolve
    participant session_progress_file_parent_mkdir as session_progress_file.parent.mkdir
    participant _json_dump as _json.dump
    participant session_hooks
    participant summary_script_exists as summary_script.exists
    participant session_chain_manager as session-chain-manager
    participant chain_script_exists as chain_script.exists
    participant hook_data_get as hook_data.get
    participant hook_input_get as hook_input.get
    participant cleanup_window
    participant sessions_dir_exists as sessions_dir.exists
    participant sessions_dir_glob as sessions_dir.glob
    participant _glob_glob as _glob.glob
    participant flags_dir_glob as flags_dir.glob
    participant random_choice as random.choice
    participant unlink
    participant dir
    participant generate_mcp_docs as generate-mcp-docs
    participant ast_walk as ast.walk
    participant filepath_read_text as filepath.read_text
    participant Version
    participant ast_get_docstring as ast.get_docstring
    participant docstring_split as docstring.split
    participant filepath_stem_replace as filepath.stem.replace
    participant splitlines
    participant line_index as line.index
    participant sorted
    participant MCP_DIR_glob as MCP_DIR.glob
    participant output_file_parent_mkdir as output_file.parent.mkdir
    participant output_file_write_text as output_file.write_text
    participant github_issue_manager
    participant SESSION_STATE_FILE_exists as SESSION_STATE_FILE.exists
    participant mapping_file_exists as mapping_file.exists
    participant mapping_file_parent_mkdir as mapping_file.parent.mkdir
    participant mapping_get as mapping.get
    participant session_ops_get as session_ops.get
    participant content_lower as content.lower
    participant tool_response_get as tool_response.get
    participant content_lower_find as content_lower.find
    participant match_group as match.group
    participant content_split as content.split
    participant ch_isdigit as ch.isdigit
    participant item_get as item.get
    participant trace_file_exists as trace_file.exists
    participant tracker_log_exists as tracker_log.exists
    participant entry_get as entry.get
    participant _debug_log_gh
    participant flow_ctx_get as flow_ctx.get
    participant progress_ctx_get as progress_ctx.get
    participant log_path_parent_mkdir as log_path.parent.mkdir
    participant description_split as description.split
    participant remote_result_stdout_strip as remote_result.stdout.strip
    participant result_stderr_lower as result.stderr.lower
    participant num_str_isdigit as num_str.isdigit
    participant flow_ctx_keys as flow_ctx.keys
    participant progress_ctx_keys as progress_ctx.keys
    participant sys_stdout_write as sys.stdout.write
    participant sys_stdout_flush as sys.stdout.flush
    participant issue_type_capitalize as issue_type.capitalize
    participant branch_result_stdout_strip as branch_result.stdout.strip
    participant remote_url_rsplit as remote_url.rsplit
    participant issue_url_rsplit as issue_url.rsplit
    participant CommitMessageBuilder
    participant traceback_format_exc as traceback.format_exc
    participant issue_data_get as issue_data.get
    participant activity_get as activity.get
    participant _MemoryLayer
    participant datetime_strptime as datetime.strptime
    participant delta_total_seconds as delta.total_seconds
    participant items
    participant lower
    participant any
    participant LABEL_DEFINITIONS_get as LABEL_DEFINITIONS.get
    participant existing_add as existing.add
    participant labels_extend as labels.extend
    participant type_map_get as type_map.get
    participant text_lower as text.lower
    participant slug_strip as slug.strip
    participant ch_isalnum as ch.isalnum
    participant cut_rfind as cut.rfind
    participant verify_result_stdout_strip as verify_result.stdout.strip
    participant log_dir_mkdir as log_dir.mkdir
    participant max
    participant sessions_dir_iterdir as sessions_dir.iterdir
    participant f_is_dir as f.is_dir
    participant f_name_startswith as f.name.startswith
    participant f_stat as f.stat
    participant branch_startswith as branch.startswith
    participant github_pr_workflow
    participant log_file_parent_mkdir as log_file.parent.mkdir
    participant summary_file_exists as summary_file.exists
    participant branch_name_startswith as branch_name.startswith
    participant branch_name_rsplit as branch_name.rsplit
    participant isdigit
    participant numbers_add as numbers.add
    participant bool
    participant os_path_dirname as os.path.dirname
    participant session_summary_get as session_summary.get
    participant ErrorMessages
    participant os_path_abspath as os.path.abspath
    participant llm_call
    participant req_get as req.get
    participant diff_result_stdout_strip as diff_result.stdout.strip
    participant pr_url_rsplit as pr_url.rsplit
    participant f_strip as f.strip
    participant file_path_lower as file_path.lower
    participant name_lower as name.lower
    participant suffix_lower as suffix.lower
    participant ext_map_get as ext_map.get
    participant t_lower as t.lower
    participant review_findings_items as review_findings.items
    participant comment_parts_extend as comment_parts.extend
    participant file_path_endswith as file_path.endswith
    participant build_result_get as build_result.get
    participant progress_get as progress.get
    participant tool_counts_values as tool_counts.values
    participant tool_counts_items as tool_counts.items
    participant current_version_split as current_version.split
    participant version_file_write_text as version_file.write_text
    participant changelog_file_exists as changelog_file.exists
    participant version_file_exists as version_file.exists
    participant changelog_file_read_text as changelog_file.read_text
    participant content_find as content.find
    participant version_file_read_text as version_file.read_text
    participant changelog_file_write_text as changelog_file.write_text
    participant line_split as line.split
    participant status_symbol_get as status_symbol.get
    participant ide_paths
    participant SCRIPTS_DIR_mkdir as SCRIPTS_DIR.mkdir
    participant POLICIES_DIR_mkdir as POLICIES_DIR.mkdir
    participant SKILLS_DIR_mkdir as SKILLS_DIR.mkdir
    participant AGENTS_DIR_mkdir as AGENTS_DIR.mkdir
    participant MEMORY_BASE_mkdir as MEMORY_BASE.mkdir
    participant mkdir
    participant CONFIG_DIR_mkdir as CONFIG_DIR.mkdir
    participant FLAG_DIR_mkdir as FLAG_DIR.mkdir
    participant POLICY_LEVELS_values as POLICY_LEVELS.values
    participant policy_dir_mkdir as policy_dir.mkdir
    participant __import__
    participant mcp_hook_integration
    participant trace_data_get as trace_data.get
    participant flag_file_exists as flag_file.exists
    participant flag_file_read_text as flag_file.read_text
    participant config_get as config.get
    participant metrics_dir_mkdir as metrics_dir.mkdir
    participant metrics_emitter as metrics-emitter
    participant p_parent_mkdir as p.parent.mkdir
    participant datetime_utcnow as datetime.utcnow
    participant record_setdefault as record.setdefault
    participant fh_write as fh.write
    participant record_update as record.update
    participant extra_items as extra.items
    participant details_items as details.items
    participant round
    participant float
    participant sid_startswith as sid.startswith
    participant session_file_read_text as session_file.read_text
    participant session_dir_mkdir as session_dir.mkdir
    participant flow_trace_file_exists as flow_trace_file.exists
    participant flow_trace_file_write_text as flow_trace_file.write_text
    participant flow_trace_file_read_text as flow_trace_file.read_text
    participant flow_trace_get as flow_trace.get
    participant p_get as p.get
    participant post_merge_version_updater as post-merge-version-updater
    participant msg_lower as msg.lower
    participant current_split as current.split
    participant commit_msg_startswith as commit_msg.startswith
    participant readme_file_exists as readme_file.exists
    participant readme_file_read_text as readme_file.read_text
    participant content_replace as content.replace
    participant re_sub as re.sub
    participant readme_file_write_text as readme_file.write_text
    participant policy_script_exists as policy_script.exists
    participant result_split as result.split
    participant post_tool_tracker as post-tool-tracker
    participant legacy_exists as legacy.exists
    participant session_flag_exists as session_flag.exists
    participant legacy_path_exists as legacy_path.exists
    participant _flag_glob_glob as _flag_glob.glob
    participant pattern_prefix_lstrip as pattern_prefix.lstrip
    participant session_flag_unlink as session_flag.unlink
    participant legacy_path_unlink as legacy_path.unlink
    participant min
    participant tool_counts_get as tool_counts.get
    participant REQUIRED_KEYS_issubset as REQUIRED_KEYS.issubset
    participant data_keys as data.keys
    participant SESSION_STATE_FILE_parent_mkdir as SESSION_STATE_FILE.parent.mkdir
    participant TRACKER_LOG_parent_mkdir as TRACKER_LOG.parent.mkdir
    participant lower_startswith as lower.startswith
    participant _failure_detector_detect_failure_in_message as _failure_detector.detect_failure_in_message
    participant _sp_run as _sp.run
    participant endswith
    participant _diff_result_stdout_strip as _diff_result.stdout.strip
    participant f_lower as f.lower
    participant l_strip as l.strip
    participant updater_script_exists as updater_script.exists
    participant _sp_merge_run as _sp_merge.run
    participant result_stdout_decode as result.stdout.decode
    participant output_get as output.get
    participant sys_stderr_write as sys.stderr.write
    participant sys_stderr_flush as sys.stderr.flush
    participant debug_log
    participant PROGRESS_DELTA_get as PROGRESS_DELTA.get
    participant inp_get as inp.get
    participant context_usage_file_parent_mkdir as context_usage_file.parent.mkdir
    participant failure_info_get as failure_info.get
    participant per_tool_get as per_tool.get
    participant flag_path_exists as flag_path.exists
    participant tool_input_keys as tool_input.keys
    participant file_path_replace as file_path.replace
    participant flag_data_get as flag_data.get
    participant _commit_check_get as _commit_check.get
    participant post_tool_tracker_mcp_server
    participant os_path_join as os.path.join
    participant work_done_flag_parent_mkdir as work_done_flag.parent.mkdir
    participant work_done_flag_write_text as work_done_flag.write_text
    participant gim__load_mapping as gim._load_mapping
    participant _subprocess_run as _subprocess.run
    participant all
    participant os_environ_copy as os.environ.copy
    participant work_done_flag_exists as work_done_flag.exists
    participant d_get as d.get
    participant task_issues_values as task_issues.values
    participant pre_tool_enforcer as pre-tool-enforcer
    participant kb_path_exists as kb_path.exists
    participant kb_get as kb.get
    participant pattern_get as pattern.get
    participant solution_get as solution.get
    participant tool_input_get as tool_input.get
    participant mapping_items as mapping.items
    participant cmd_startswith as cmd.startswith
    participant win_cmd_lower as win_cmd.lower
    participant progress_file_exists as progress_file.exists
    participant session_flag_path_exists as session_flag_path.exists
    participant legacy_files_sort as legacy_files.sort
    participant os_path_getmtime as os.path.getmtime
    participant session_flag_path_unlink as session_flag_path.unlink
    participant flag_path_unlink as flag_path.unlink
    participant raw_trace_get as raw_trace.get
    participant flag_dir_exists as flag_dir.exists
    participant trace_path_exists as trace_path.exists
    participant flag_dir_glob as flag_dir.glob
    participant trace_path_read_text as trace_path.read_text
    participant _trace_path_l1_exists as _trace_path_l1.exists
    participant _trace_path_l2_exists as _trace_path_l2.exists
    participant command_strip as command.strip
    participant cmd_stripped_lower as cmd_stripped.lower
    participant cmd_lower_startswith as cmd_lower.startswith
    participant _branch_result_stdout_strip as _branch_result.stdout.strip
    participant cmd_lower_strip as cmd_lower.strip
    participant win_cmd_strip as win_cmd.strip
    participant repr
    participant blocks_extend as blocks.extend
    participant os_path_getsize as os.path.getsize
    participant ctx_get as ctx.get
    participant FILE_EXT_SKILL_MAP_get as FILE_EXT_SKILL_MAP.get
    participant filename_lower as filename.lower
    participant normalized_lower as normalized.lower
    participant normalized_rsplit as normalized.rsplit
    participant pattern_name_lower as pattern_name.lower
    participant normalized_split as normalized.split
    participant trace_context_get as trace_context.get
    participant filename_rsplit as filename.rsplit
    participant _optimizer_optimize as _optimizer.optimize
    participant _pre_checker_check_tool_call as _pre_checker.check_tool_call
    participant check_result_get as check_result.get
    participant changes_items as changes.items
    participant issue_get as issue.get
    participant all_hints_extend as all_hints.extend
    participant all_blocks_extend as all_blocks.extend
    participant token_optimization_mcp_server
    participant load_result_get as load_result.get
    participant skill_manager_mcp_server
    participant release
    participant VERSION_FILE_exists as VERSION_FILE.exists
    participant VERSION_FILE_read_text as VERSION_FILE.read_text
    participant CHANGELOG_FILE_is_file as CHANGELOG_FILE.is_file
    participant CHANGELOG_FILE_read_text as CHANGELOG_FILE.read_text
    participant existing_find as existing.find
    participant CHANGELOG_FILE_write_text as CHANGELOG_FILE.write_text
    participant SYNC_SCRIPT_exists as SYNC_SCRIPT.exists
    participant VERSION_FILE_write_text as VERSION_FILE.write_text
    participant a_startswith as a.startswith
    participant CHAIN_LOG_parent_mkdir as CHAIN_LOG.parent.mkdir
    participant CHAIN_INDEX_FILE_exists as CHAIN_INDEX_FILE.exists
    participant data_setdefault as data.setdefault
    participant CHAIN_INDEX_FILE_parent_mkdir as CHAIN_INDEX_FILE.parent.mkdir
    participant parent_session_file_exists as parent_session_file.exists
    participant parent_session_file_read_text as parent_session_file.read_text
    participant parent_entry_get as parent_entry.get
    participant pdata_get as pdata.get
    participant tag_strip as tag.strip
    participant ancestor_get as ancestor.get
    participant tag_matches_values as tag_matches.values
    participant sess_get as sess.get
    participant rel_get as rel.get
    participant insert
    participant queue_pop as queue.pop
    participant visited_add as visited.add
    participant queue_extend as queue.extend
    participant anc_get as anc.get
    participant tr_get as tr.get
    participant prompt_lower as prompt.lower
    participant tech_keywords_items as tech_keywords.items
    participant reversed
    participant skill_lower as skill.lower
    participant seen_add as seen.add
    participant part_startswith as part.startswith
    participant t_strip as t.strip
    participant part_lower as part.lower
    participant task_type_lower as task_type.lower
    participant candidate_sessions_add as candidate_sessions.add
    participant setdefault
    participant parser_add_subparsers as parser.add_subparsers
    participant subparsers_add_parser as subparsers.add_parser
    participant link_parser_add_argument as link_parser.add_argument
    participant tag_parser_add_argument as tag_parser.add_argument
    participant relate_parser_add_argument as relate_parser.add_argument
    participant ctx_parser_add_argument as ctx_parser.add_argument
    participant chain_parser_add_argument as chain_parser.add_argument
    participant search_parser_add_argument as search_parser.add_argument
    participant autotag_parser_add_argument as autotag_parser.add_argument
    participant parser_print_help as parser.print_help
    participant args_tags_split as args.tags.split
    participant self_sessions_dir_mkdir as self.sessions_dir.mkdir
    participant self_sessions_log_parent_mkdir as self.sessions_log.parent.mkdir
    participant now_strftime as now.strftime
    participant random_choices as random.choices
    participant self_current_session_file_exists as self.current_session_file.exists
    participant self_current_session_file_unlink as self.current_session_file.unlink
    participant session_data_get as session_data.get
    participant self_sessions_dir_glob as self.sessions_dir.glob
    participant x_stat as x.stat
    participant duration_total_seconds as duration.total_seconds
    participant session_id_generator as session-id-generator
    participant session_get as session.get
    participant session_summary_manager as session-summary-manager
    participant SUMMARY_LOG_parent_mkdir as SUMMARY_LOG.parent.mkdir
    participant json_path_exists as json_path.exists
    participant supplementary_skills_split as supplementary_skills.split
    participant s_strip as s.strip
    participant datetime
    participant e_get as e.get
    participant cmd_strip as cmd.strip
    participant tracker_file_exists as tracker_file.exists
    participant flow_trace_path_exists as flow_trace_path.exists
    participant trace_get as trace.get
    participant session_json_get as session_json.get
    participant md_path_write_text as md_path.write_text
    participant script_inv_get as script_inv.get
    participant policy_exec_data_get as policy_exec_data.get
    participant inv_summary_get as inv_summary.get
    participant summary_data_get as summary_data.get
    participant baseline_file_parent_mkdir as baseline_file.parent.mkdir
    participant SESSIONS_DIR_glob as SESSIONS_DIR.glob
    participant old_session_file_unlink as old_session_file.unlink
    participant p_stat as p.stat
    participant glob
    participant shutil_rmtree as shutil.rmtree
    participant json_path_parent_mkdir as json_path.parent.mkdir
    participant values
    participant tool_stats_get as tool_stats.get
    participant flow_decision_get as flow_decision.get
    participant tool_opt_stats_get as tool_opt_stats.get
    participant policy_exec_summary_get as policy_exec_summary.get
    participant err_get as err.get
    participant zip
    participant story_get as story.get
    participant pol_get as pol.get
    participant per_tool_counts_items as per_tool_counts.items
    participant s_get as s.get
    participant inv_get as inv.get
    participant x_get as x.get
    participant decision_get as decision.get
    participant slowest_top_get as slowest_top.get
    participant fastest_top_get as fastest_top.get
    participant chain_index_file_exists as chain_index_file.exists
    participant chain_get as chain.get
    participant md_path_exists as md_path.exists
    participant md_path_read_text as md_path.read_text
    participant acc_add_argument as acc.add_argument
    participant fin_add_argument as fin.add_argument
    participant rd_add_argument as rd.add_argument
    participant SessionIndex
    participant self_index_file_exists as self.index_file.exists
    participant self_sessions_dir_iterdir as self.sessions_dir.iterdir
    participant self_sessions_dir_exists as self.sessions_dir.exists
    participant project_dir_is_dir as project_dir.is_dir
    participant project_dir_glob as project_dir.glob
    participant project_dir_stat as project_dir.stat
    participant self_index_file_parent_mkdir as self.index_file.parent.mkdir
    participant SessionAPI
    participant self_index_projects_get as self.index.projects.get
    participant session_file_stat as session_file.stat
    participant self_archive_dir_mkdir as self.archive_dir.mkdir
    participant session_file_rename as session_file.rename
    participant f_readlines as f.readlines
    participant f_writelines as f.writelines
    participant stop_notifier as stop-notifier
    participant STOP_LOG_parent_mkdir as STOP_LOG.parent.mkdir
    participant flag_path_read_text as flag_path.read_text
    participant flag_path_write_text as flag_path.write_text
    participant raw_startswith as raw.startswith
    participant prog_get as prog.get
    participant encode
    participant urllib_request_Request as urllib_request.Request
    participant urllib_request_urlopen as urllib_request.urlopen
    participant decode
    participant message_startswith as message.startswith
    participant message_endswith as message.endswith
    participant resp_read as resp.read
    participant message_replace as message.replace
    participant VOICE_SCRIPT_exists as VOICE_SCRIPT.exists
    participant subprocess_Popen as subprocess.Popen
    participant text_strip as text.strip
    participant get_default_fn
    participant sessions_base_exists as sessions_base.exists
    participant session_dir_exists as session_dir.exists
    participant sessions_base_iterdir as sessions_base.iterdir
    participant d_is_dir as d.is_dir
    participant d_stat as d.stat
    participant user_msg_file_exists as user_msg_file.exists
    participant system_prompt_file_exists as system_prompt_file.exists
    participant prefix_map_get as prefix_map.get
    participant github_mcp_server
    participant ahead_result_stdout_strip as ahead_result.stdout.strip
    participant summary_file_read_text as summary_file.read_text
    participant summary_text_split as summary_text.split
    participant system_prompt_file_read_text as system_prompt_file.read_text
    participant user_msg_file_read_text as user_msg_file.read_text
    participant user_msg_split as user_msg.split
    participant part_split as part.split
    participant step8_data_get as step8_data.get
    participant result_summary_get as result_summary.get
    participant step8_log_exists as step8_log.exists
    participant step8_log_read_text as step8_log.read_text
    participant sync_script_exists as sync_script.exists
    participant summary_file_write_text as summary_file.write_text
    participant _resolve_flag
    participant git_commit_script_exists as git_commit_script.exists
    participant save_script_exists as save_script.exists
    participant archive_script_exists as archive_script.exists
    participant pruner_script_exists as pruner_script.exists
    participant failure_script_exists as failure_script.exists
    participant pref_script_exists as pref_script.exists
    participant plan_archiver_script_exists as plan_archiver_script.exists
    participant pid_flag_exists as pid_flag.exists
    participant legacy_flag_exists as legacy_flag.exists
    participant retry_flag_write_text as retry_flag.write_text
    participant _retry_branch_stdout_strip as _retry_branch.stdout.strip
    participant session_issues_file_exists as session_issues_file.exists
    participant session_issues_file_read_text as session_issues_file.read_text
    participant status_result_stdout_strip as status_result.stdout.strip
    participant time_time as time.time
    participant ready_flag_exists as ready_flag.exists
    participant ready_flag_unlink as ready_flag.unlink
    participant ready_flag_write_text as ready_flag.write_text
    participant ready_flag_read_text as ready_flag.read_text
    participant sync_insight as sync-insight
    participant hook_downloader_exists as hook_downloader.exists
    participant sync_library as sync-library
    participant sync_version as sync-version
    participant filepath_exists as filepath.exists
    participant replacement_replace as replacement.replace
    participant filepath_write_text as filepath.write_text
    participant TraceIndex
    participant self_trace_file_exists as self.trace_file.exists
    participant TraceAPI
    participant trace_api
    participant update_status as update-status
    participant voice_notifier as voice-notifier
    participant VOICE_LOG_parent_mkdir as VOICE_LOG.parent.mkdir
    participant winsound_PlaySound as winsound.PlaySound
    participant tempfile_mkstemp as tempfile.mkstemp
    participant os_close as os.close
    participant asyncio_run as asyncio.run
    participant edge_tts_Communicate as edge_tts.Communicate
    participant _generate
    participant communicate_save as communicate.save
    participant wav_path_replace as wav_path.replace
    participant os_unlink as os.unlink
    participant shutil_copy2 as shutil.copy2
    participant time_sleep as time.sleep
    participant tempfile_gettempdir as tempfile.gettempdir
    participant COQUI_CACHE_DIR_mkdir as COQUI_CACHE_DIR.mkdir
    participant TTS
    participant tts_tts_to_file as tts.tts_to_file
    participant pyttsx3_init as pyttsx3.init
    participant engine_getProperty as engine.getProperty
    participant engine_setProperty as engine.setProperty
    participant engine_say as engine.say
    participant engine_runAndWait as engine.runAndWait
    participant voice_name_lower as voice.name.lower
    participant text_replace as text.replace
    participant result_stderr_decode as result.stderr.decode
    participant conftest
    participant pytest_fixture as pytest.fixture
    participant MagicMock
    participant widget_dir_mkdir as widget_dir.mkdir
    participant item_add_marker as item.add_marker
    participant pytest_mark_xfail as pytest.mark.xfail
    participant quick_test
    participant unittest_TestLoader as unittest.TestLoader
    participant unittest_TestSuite as unittest.TestSuite
    participant unittest_TextTestRunner as unittest.TextTestRunner
    participant runner_run as runner.run
    participant result_wasSuccessful as result.wasSuccessful
    participant suite_addTests as suite.addTests
    participant loader_loadTestsFromModule as loader.loadTestsFromModule
    participant test_name_startswith as test_name.startswith
    participant test_dir_glob as test_dir.glob
    participant run_all_tests
    participant SkillRegistry
    participant count_tests
    participant coverage_Coverage as coverage.Coverage
    participant cov_start as cov.start
    participant cov_stop as cov.stop
    participant cov_save as cov.save
    participant anti_hallucination_enforcement as anti-hallucination-enforcement
    participant cov_html_report as cov.html_report
    participant test_parallel_mode as test-parallel-mode
    participant doc_path_exists as doc_path.exists
    participant f_read as f.read
    participant hooks_get as hooks.get
    participant test_prompt_generator as test-prompt-generator
    participant step_get as step.get
    participant test_window_isolation as test-window-isolation
    participant flag_pid1_write_text as flag_pid1.write_text
    participant flag_pid_current_write_text as flag_pid_current.write_text
    participant flag_pid1_exists as flag_pid1.exists
    participant flag_pid1_unlink as flag_pid1.unlink
    participant flag_pid_current_unlink as flag_pid_current.unlink
    participant flag_pid_current_read_text as flag_pid_current.read_text
    participant data_current_get as data_current.get
    participant flag_pid1_read_text as flag_pid1.read_text
    participant data_other_get as data_other.get
    participant registry_path_exists as registry_path.exists
    participant registry_path_read_text as registry_path.read_text
    participant test_func
    participant test_architecture_smoke
    participant level_path_exists as level_path.exists
    participant level_path_rglob as level_path.rglob
    participant p_name_startswith as p.name.startswith
    participant importlib_util_spec_from_file_location as importlib.util.spec_from_file_location
    participant importlib_util_module_from_spec as importlib.util.module_from_spec
    participant spec_loader_exec_module as spec.loader.exec_module
    participant py_file_stem_replace as py_file.stem.replace
    participant TestArchitectureSmokeImports
    participant ARCH_DIR_exists as ARCH_DIR.exists
    participant ARCH_DIR_is_dir as ARCH_DIR.is_dir
    participant level_path_is_dir as level_path.is_dir
    participant pytest_skip as pytest.skip
    participant pytest_mark_parametrize as pytest.mark.parametrize
    participant ARCH_DIR_rglob as ARCH_DIR.rglob
    participant TestBulletproofMergeDetection
    participant patch
    participant Level3GitHubWorkflow
    participant patch_object as patch.object
    participant workflow_check_merge_conflicts_bulletproof as workflow.check_merge_conflicts_bulletproof
    participant workflow__detect_project_type as workflow._detect_project_type
    participant TestBulletproofSummary
    participant test_cache_system
    participant types_ModuleType as types.ModuleType
    participant attrs_items as attrs.items
    participant setattr
    participant TestMemoryLayerSetGet
    participant layer_set as layer.set
    participant layer_get as layer.get
    participant TestMemoryLayerTtlExpiry
    participant TestMemoryLayerLruEviction
    participant TestCacheTierGetMiss
    participant CacheTier
    participant tier_get as tier.get
    participant TestCacheTierSetGetHit
    participant tier_set as tier.set
    participant TestCacheTierDiskPersistence
    participant tier__mem_clear as tier._mem.clear
    participant tier__mem_get as tier._mem.get
    participant TestCacheTierInvalidate
    participant tier_invalidate as tier.invalidate
    participant TestCacheTierClearExpired
    participant stale_path_write_text as stale_path.write_text
    participant stale_path_exists as stale_path.exists
    participant TestCacheTierHitRate
    participant TestCacheTierStats
    participant tier_stats as tier.stats
    participant TestMakeLlmKeyDeterministic
    participant TestMakeFileKeyIncludesMtime
    participant f_write_text as f.write_text
    participant TestPipelineCacheThreeTiers
    participant PipelineCache
    participant TestGetPipelineCacheSingleton
    participant cache_system
    participant TestCachedLlmCallUsesCache
    participant TestCachedFileRead
    participant test_call_graph_analyzer
    participant parent_mkdir as parent.mkdir
    participant write_text
    participant TestAnalyzeImpactBeforeChange
    participant call_graph_analyzer
    participant fqn_split as fqn.split
    participant TestGetImplementationContext
    participant deps_items as deps.items
    participant TestReviewChangeImpact
    participant pre_snapshot_get as pre_snapshot.get
    participant TestSnapshotCallGraph
    participant stats_keys as stats.keys
    participant test_call_graph_builder
    participant TestDataStructures
    participant call_graph_builder
    participant TestCallGraphVisitor
    participant graph_classes_values as graph.classes.values
    participant graph_methods_items as graph.methods.items
    participant graph_methods_values as graph.methods.values
    participant TestEdgeResolution
    participant CallGraph
    participant TestCallPaths
    participant TestImpactAnalysis
    participant TestCallGraphStats
    participant graph_get_stats as graph.get_stats
    participant TestSerialization
    participant graph_to_dict as graph.to_dict
    participant graph_to_json as graph.to_json
    participant TestIntegration
    participant TestCyclomaticComplexity
    participant TestParamsAndTypes
    participant TestHelpers
    participant ast_Name as ast.Name
    participant ast_Constant as ast.Constant
    participant TestEdgeCases
    participant test_checkpoint_manager
    participant CheckpointManager
    participant path_write_text as path.write_text
    participant TestCheckpointManagerInit
    participant mgr_checkpoint_dir_exists as mgr.checkpoint_dir.exists
    participant mgr_checkpoint_dir_is_dir as mgr.checkpoint_dir.is_dir
    participant TestSaveCheckpoint
    participant read_text
    participant TestLoadCheckpoint
    participant TestLoadCheckpointById
    participant TestGetLastCheckpoint
    participant TestGetLastSuccessfulCheckpoint
    participant TestListCheckpoints
    participant TestDeleteCheckpoint
    participant TestClearAll
    participant mgr_checkpoint_dir_glob as mgr.checkpoint_dir.glob
    participant TestAtomicWriteIntegrity
    participant E2EWorkflowTest
    participant tempfile_mkdtemp as tempfile.mkdtemp
    participant project_root_mkdir as project_root.mkdir
    participant level3_execution
    participant state_update as state.update
    participant InferenceRouter
    participant step_func
    participant step_name_lower as step_name.lower
    participant test_end_to_end_workflow
    participant test_enforcement_mcp_server
    participant TestCheckEnforcementStatus
    participant enforcement_mcp_server
    participant state_file_write_text as state_file.write_text
    participant TestEnforcePolicyStep
    participant state_file_read_text as state_file.read_text
    participant TestLogToolUsage
    participant log_dir_glob as log_dir.glob
    participant TestVerifyCompliance
    participant TestListPolicies
    participant TestJsonFormat
    participant tool_fn
    participant test_enhancements_v75
    participant TestCheckpointerManager
    participant CheckpointerManager
    participant tempfile_TemporaryDirectory as tempfile.TemporaryDirectory
    participant db_path_exists as db_path.exists
    participant TestSystemHealthCheck
    participant TestLLMHealthCheck
    participant llm_mcp_server
    participant expected_issubset as expected.issubset
    participant providers_keys as providers.keys
    participant result1_get as result1.get
    participant result2_get as result2.get
    participant TestDynamicStandardsReload
    participant standards_loader_mcp_server
    participant d_exists as d.exists
    participant TestOrchestratorCheckpointer
    participant test_error_infrastructure
    participant ErrorLogger
    participant logger_log_decision as logger.log_decision
    participant audit_path_exists as audit_path.exists
    participant BackupManager
    participant tempfile_NamedTemporaryFile as tempfile.NamedTemporaryFile
    participant test_file_write_text as test_file.write_text
    participant test_file_read_text as test_file.read_text
    participant test_file_unlink as test_file.unlink
    participant GithubException
    participant super
    participant TestGitHubMCP
    participant Mock
    participant GitHubMCP
    participant mock_github_mcp_create_issue as mock_github_mcp.create_issue
    participant mock_github_mcp_repo_create_issue_assert_called_once as mock_github_mcp.repo.create_issue.assert_called_once
    participant GitHubMCP_create_issue___get__ as GitHubMCP.create_issue.__get__
    participant mcp_create_issue as mcp.create_issue
    participant mock_github_mcp_add_issue_comment as mock_github_mcp.add_issue_comment
    participant mock_github_mcp_repo_get_issue_assert_called_once_with as mock_github_mcp.repo.get_issue.assert_called_once_with
    participant mock_github_mcp_close_issue as mock_github_mcp.close_issue
    participant mock_issue_edit_assert_called_once as mock_issue.edit.assert_called_once
    participant mock_github_mcp_create_pull_request as mock_github_mcp.create_pull_request
    participant mock_github_mcp_repo_create_pull_assert_called_once as mock_github_mcp.repo.create_pull.assert_called_once
    participant mock_github_mcp_merge_pull_request as mock_github_mcp.merge_pull_request
    participant mock_pr_merge_assert_called_once as mock_pr.merge.assert_called_once
    participant mock_github_mcp_add_pr_comment as mock_github_mcp.add_pr_comment
    participant TestGitHubOperationRouter
    participant GitHubOperationRouter
    participant mock_router_create_issue as mock_router.create_issue
    participant mock_router_gh_cli_create_issue_assert_not_called as mock_router.gh_cli.create_issue.assert_not_called
    participant mock_router_mcp_create_issue_assert_called_once as mock_router.mcp.create_issue.assert_called_once
    participant mock_router_gh_cli_create_issue_assert_called_once as mock_router.gh_cli.create_issue.assert_called_once
    participant mock_router_mcp_create_issue_assert_not_called as mock_router.mcp.create_issue.assert_not_called
    participant mock_router_merge_pull_request as mock_router.merge_pull_request
    participant mock_router_gh_cli_merge_pull_request_assert_called_once as mock_router.gh_cli.merge_pull_request.assert_called_once
    participant callable
    participant test_github_mcp_server
    participant TestRepoDetection
    participant GitHubIntegration
    participant TestTokenResolution
    participant patch_dict as patch.dict
    participant _get_token
    participant TestGitHubIssues
    participant TestGitHubPRs
    participant _json
    participant test_github_router_real
    participant os_getenv as os.getenv
    participant result_stdout_split as result.stdout.split
    participant center
    participant test_git_mcp_server
    participant TestGitStatus
    participant git_mcp_server
    participant TestGitBranchList
    participant TestGitLog
    participant TestGitDiff
    participant TestGitStash
    participant TestGitCommit
    participant status_get as status.get
    participant TestGitFetch
    participant TestAuthenticationFlow
    participant self_app_test_client as self.app.test_client
    participant self_client_get as self.client.get
    participant self_client_post as self.client.post
    participant self_client_session_transaction as self.client.session_transaction
    participant TestDashboardDataFlow
    participant TestMonitoringPipeline
    participant MetricsCollector
    participant LogParser
    participant PolicyChecker
    participant TestAIPipeline
    participant AnomalyDetector
    participant PredictiveAnalytics
    participant BottleneckAnalyzer
    participant detector_detect_anomalies as detector.detect_anomalies
    participant analyzer_analyze_performance as analyzer.analyze_performance
    participant analytics_predict_future_usage as analytics.predict_future_usage
    participant TestNotificationPipeline
    participant NotificationManager
    participant AlertRoutingEngine
    participant AlertSender
    participant manager_create_notification as manager.create_notification
    participant router_add_routing_rule as router.add_routing_rule
    participant router_route_alert as router.route_alert
    participant TestWidgetWorkflow
    participant CommunityWidgetsManager
    participant WidgetVersionManager
    participant community_list_community_widgets as community.list_community_widgets
    participant community_install_widget as community.install_widget
    participant versions_create_version as versions.create_version
    participant TestExportWorkflow
    participant TestUserJourney
    participant TestErrorRecovery
    participant Exception
    participant self_collector_get_system_health as self.collector.get_system_health
    participant test_integration_all_mcp
    participant TestImportHealth
    participant TestToolCounts
    participant attr_startswith as attr.startswith
    participant TestCrossServerIntegration
    participant mod_detect_project_type as mod.detect_project_type
    participant pre_tool_gate_mcp_server
    participant TestASTNavigation
    participant TestContextDeduplication
    participant mod_deduplicate_context as mod.deduplicate_context
    participant TestSmartRead
    participant TestSkillManagerFunctional
    participant skill_dir_mkdir as skill_dir.mkdir
    participant d_mkdir as d.mkdir
    participant TestFlowStateInitialization
    participant TestLevel1SyncExecution
    participant level1_sync
    participant TestLevel2StandardsExecution
    participant level2_standards
    participant mock_load_assert_called_once as mock_load.assert_called_once
    participant mock_script_assert_called_once as mock_script.assert_called_once
    participant TestLevel3ExecutionSystem
    participant TestPolicyScriptExecution
    participant TestFlowTraceJsonFormat
    participant TestEndToEndFlowExecution
    participant TestErrorHandling
    participant TimeoutError
    participant TestContextThresholdRouting
    participant TestAntiHallucinationWiring
    participant call_log_index as call_log.index
    participant TestModelTierRouting
    participant call_kwargs_kwargs_get as call_kwargs.kwargs.get
    participant env_get as env.get
    participant os_environ_pop as os.environ.pop
    participant TestPolicyScriptIntegration
    participant arch_dir_exists as arch_dir.exists
    participant level1_dir_exists as level1_dir.exists
    participant level1_dir_glob as level1_dir.glob
    participant level2_dir_exists as level2_dir.exists
    participant level2_dir_glob as level2_dir.glob
    participant level3_dir_exists as level3_dir.exists
    participant level3_dir_glob as level3_dir.glob
    participant level3_dir_iterdir as level3_dir.iterdir
    participant level_dir_exists as level_dir.exists
    participant level_dir_glob as level_dir.glob
    participant TestPolicyScriptOutput
    participant TestFlowTraceOutputFormat
    participant test_level1_foundations
    participant TestTOONValidator
    participant toon_schema
    participant TestComplexityCalculator
    participant complexity_calculator
    participant TestContextTimeout
    participant original_read_text
    participant pytest_raises as pytest.raises
    participant inspect_getsource as inspect.getsource
    participant TestTOONCompression
    participant toon_file_exists as toon_file.exists
    participant toon_file_read_text as toon_file.read_text
    participant toon_get as toon.get
    participant mock_validate_assert_called_once as mock_validate.assert_called_once
    participant TestMemoryLimits
    participant large_srs_write_bytes as large_srs.write_bytes
    participant context_deduplicator
    participant TestPartialContextFallback
    participant srs_dir_mkdir as srs_dir.mkdir
    participant context_data_get as context_data.get
    participant original_read
    participant TestCacheInvalidation
    participant ContextCache
    participant loaded_get as loaded.get
    participant CC__cache_key as CC._cache_key
    participant cache_file_write_text as cache_file.write_text
    participant cache_file_read_text as cache_file.read_text
    participant readme_write_text as readme.write_text
    participant ContextCache__cache_key as ContextCache._cache_key
    participant cache_invalidate as cache.invalidate
    participant ContextCache_get_session_stats as ContextCache.get_session_stats
    participant TestLevel1Integration
    participant merge_result_get as merge_result.get
    participant session_result_get as session_result.get
    participant complexity_result_get as complexity_result.get
    participant ctx_result_get as ctx_result.get
    participant toon_result_get as toon_result.get
    participant test_level1_optimization
    participant CACHE_STATS_reset as CACHE_STATS.reset
    participant cache_dir_mkdir as cache_dir.mkdir
    participant TestCacheStats
    participant CacheStats
    participant stats_to_dict as stats.to_dict
    participant pytest_approx as pytest.approx
    participant stats_reset as stats.reset
    participant t_start as t.start
    participant t_join as t.join
    participant stats_file_exists as stats_file.exists
    participant stats_file_read_text as stats_file.read_text
    participant TestCacheKeyHashing
    participant hexdigest
    participant expected_full_startswith as expected_full.startswith
    participant hashlib_sha256 as hashlib.sha256
    participant resolved_encode as resolved.encode
    participant TestContextCacheLoadSave
    participant CACHE_STATS_to_dict as CACHE_STATS.to_dict
    participant TestStreamFileHead
    participant TestReadFileWithTimeout
    participant TestNodeContextLoaderOptimizations
    participant large_readme_write_text as large_readme.write_text
    participant w_lower as w.lower
    participant claude_write_text as claude.write_text
    participant TestPerformanceBenchmarks
    participant time_perf_counter as time.perf_counter
    participant IOError
    participant p_read_text as p.read_text
    participant TestFlowStateOptimizationFields
    participant source_lower as source.lower
    participant test_level1_sync
    participant base_update as base.update
    participant TestNodeSessionLoader
    participant meta_file_exists as meta_file.exists
    participant meta_file_read_text as meta_file.read_text
    participant TestNodeComplexityCalculation
    participant TestNodeContextLoader
    participant TestNodeToonCompression
    participant TestLevel1MergeNode
    participant TestCleanupLevel1Memory
    participant test_level2_standards
    participant TestLoadPoliciesFromDirectory
    participant level2_dir_mkdir as level2_dir.mkdir
    participant TestDetectProjectType
    participant TestNodeCommonStandards
    participant TestNodeJavaStandards
    participant TestNodeToolOptimizationStandards
    participant TestNodeMCPPluginDiscovery
    participant sys_modules_pop as sys.modules.pop
    participant TestLevel2MergeNode
    participant TestRouteJavaStandards
    participant test_level3_documentation_manager
    participant Level3DocumentationManager
    participant TestDetectProjectDocs
    participant TestSummarizeExistingDocs
    participant TestCreateAllDocs
    participant TestUpdateExistingDocs
    participant TestGracefulFailure
    participant test_level3_execution_v2
    participant _infra_cache_clear as _infra_cache.clear
    participant TestRunStep
    participant level3_execution_v2
    participant mock_cp_save_checkpoint_assert_called as mock_cp.save_checkpoint.assert_called
    participant mock_metrics_record_step_assert_called as mock_metrics.record_step.assert_called
    participant TestLevel3InitNode
    participant TestStep0TaskAnalysisNode
    participant TestStep8NetworkRetry
    participant requests_RequestException as requests.RequestException
    participant ValueError
    participant TestBuildRetryHistoryContext
    participant test_level3_remaining_steps
    participant Level3RemainingSteps
    participant TestSelectPlanningModel
    participant TestStep2PlanExecution
    participant steps_step2_plan_execution as steps.step2_plan_execution
    participant TestStep3TaskBreakdown
    participant steps_step3_task_breakdown as steps.step3_task_breakdown
    participant TestStep4ToonRefinement
    participant steps_step4_toon_refinement as steps.step4_toon_refinement
    participant TestStep6SkillValidation
    participant TestStep7FinalPromptGeneration
    participant steps_inference_chat as steps.inference.chat
    participant TestStep13UpdateDocumentation
    participant ImportError
    participant TestStep14FinalSummary
    participant TestInitCreatesManagers
    participant TestStepTimeout
    participant wrapper_run as wrapper.run
    participant TestConflictResolver
    participant ConflictResolver
    participant self_resolver_resolve_standard_conflicts as self.resolver.resolve_standard_conflicts
    participant fh_read as fh.read
    participant TestReviewCriteria
    participant ReviewCriteria
    participant rule_keys as rule.keys
    participant rule_get as rule.get
    participant result_to_dict as result.to_dict
    participant test_level_minus1
    participant TestNodeUnicodeFix
    participant level_minus1
    participant io_StringIO as io.StringIO
    participant TestNodeEncodingValidation
    participant write_bytes
    participant TestNodeWindowsPathCheck
    participant TestLevelMinus1MergeNode
    participant e_lower as e.lower
    participant TestRouteAfterMerge
    participant test_llm_and_skill_routing
    participant SkillAgentLoader
    participant TestLLMRoutingNoLLMSteps
    participant mock_claude_assert_not_called as mock_claude.assert_not_called
    participant TestLLMRoutingClassificationToGPU
    participant SessionIdResolver
    participant mock_ollama_chat_assert_called_once as mock_ollama.chat.assert_called_once
    participant mock_claude_assert_called_once as mock_claude.assert_called_once
    participant TestLLMRoutingComplexToClande
    participant mock_assert_called_once as mock.assert_called_once
    participant TestLLMRoutingDeepLocal
    participant TestLLMRoutingGPUFallback
    participant ConnectionError
    participant TestModelSelectionCorrect
    participant info_get as info.get
    participant TestUnknownStepRouting
    participant TestLoadSkillFlatStructure
    participant TestLoadSkillDomainStructure
    participant TestListAllSkillsFlat
    participant flat_dir_mkdir as flat_dir.mkdir
    participant legacy_dir_mkdir as legacy_dir.mkdir
    participant TestLoadAgent
    participant agent_dir_mkdir as agent_dir.mkdir
    participant TestParseYAMLFrontmatter
    participant loader_with_dirs_parse_skill_metadata as loader_with_dirs.parse_skill_metadata
    participant TestSkillNotFound
    participant TestLoadSkillAndParseMetadata
    participant test_llm_mcp_server
    participant TestLLMGenerate
    participant TestLLMListModels
    participant test_mcp_and_github_integration
    participant TestGitHubOperationRouterMCPPrimary
    participant router_create_issue as router.create_issue
    participant mcp_create_issue_assert_called_once as mcp.create_issue.assert_called_once
    participant gh_create_issue_assert_not_called as gh.create_issue.assert_not_called
    participant router_create_pull_request as router.create_pull_request
    participant mcp_create_pull_request_assert_called_once as mcp.create_pull_request.assert_called_once
    participant gh_create_pull_request_assert_not_called as gh.create_pull_request.assert_not_called
    participant gh_create_issue_assert_called_once as gh.create_issue.assert_called_once
    participant gh_create_pull_request_assert_called_once as gh.create_pull_request.assert_called_once
    participant mcp_create_issue_assert_not_called as mcp.create_issue.assert_not_called
    participant router_merge_pull_request as router.merge_pull_request
    participant mcp_merge_pull_request_assert_called_once as mcp.merge_pull_request.assert_called_once
    participant gh_merge_pull_request_assert_called_once as gh.merge_pull_request.assert_called_once
    participant router_close_issue as router.close_issue
    participant mcp_close_issue_assert_called_once as mcp.close_issue.assert_called_once
    participant gh_close_issue_assert_not_called as gh.close_issue.assert_not_called
    participant TestIssueCreationURLParsing
    participant gi_create_issue as gi.create_issue
    participant TestPRCreationURLParsing
    participant gi_create_pull_request as gi.create_pull_request
    participant TestStep8CallsRealWorkflow
    participant workflow_mock_step8_create_issue_assert_called_once as workflow_mock.step8_create_issue.assert_called_once
    participant TestStep9CallsRealWorkflow
    participant workflow_mock_step9_create_branch_assert_called_once as workflow_mock.step9_create_branch.assert_called_once
    participant TestStep11CallsRealWorkflow
    participant workflow_mock_step11_create_pull_request_assert_called_once as workflow_mock.step11_create_pull_request.assert_called_once
    participant TestStep12CallsRealWorkflow
    participant workflow_mock_step12_close_issue_assert_called_once as workflow_mock.step12_close_issue.assert_called_once
    participant TestLevel3GitHubWorkflowDirect
    participant workflow_facade_create_issue_assert_called_once as workflow.facade.create_issue.assert_called_once
    participant router_create_issue_assert_not_called as router.create_issue.assert_not_called
    participant TestMCPConfigurationEnabled
    participant kwargs_get as kwargs.get
    participant captured_get as captured.get
    participant req_path_read_text as req_path.read_text
    participant req_path_exists as req_path.exists
    participant test_mcp_base_clients
    participant object
    participant TestLazyClientSingleton
    participant TestLazyClientInit
    participant inst_get as inst.get
    participant inst_reset as inst.reset
    participant TestLazyClientHealthCheck
    participant TestGitRepoClient
    participant GitRepoClient
    participant mock_repo_cls_assert_called_once_with as mock_repo_cls.assert_called_once_with
    participant original_import
    participant TestGitHubApiClientTokenResolution
    participant GitHubApiClient
    participant mock_run_assert_called_once as mock_run.assert_called_once
    participant environ_items as environ.items
    participant TestGitHubApiClientParseRemote
    participant TestQdrantManagerCollections
    participant QdrantManager_COLLECTIONS_keys as QdrantManager.COLLECTIONS.keys
    participant QdrantManager_COLLECTIONS_items as QdrantManager.COLLECTIONS.items
    participant test_mcp_base_decorators
    participant TestMcpToolHandlerDictReturn
    participant my_tool
    participant TestMcpToolHandlerExceptions
    participant FileNotFoundError
    participant decorators
    participant TypeError
    participant KeyError
    participant TestMcpToolHandlerStringPassthrough
    participant TestMcpToolHandlerIncludeTraceback
    participant TestMcpToolHandlerLogDuration
    participant TestMcpToolHandlerBothSyntaxes
    participant TestValidateParams
    participant add
    participant TestAtomicJsonStoreSaveLoad
    participant store_save as store.save
    participant TestAtomicJsonStoreBackup
    participant bak_path_exists as bak_path.exists
    participant bak_path_read_text as bak_path.read_text
    participant TestAtomicJsonStoreFallback
    participant bak_path_write_text as bak_path.write_text
    participant TestAtomicJsonStoreModify
    participant d_update as d.update
    participant TestAtomicJsonStoreDelete
    participant TestJsonlAppenderBasic
    participant TestJsonlAppenderFiltered
    participant TestJsonlAppenderCount
    participant TestJsonlAppenderMalformed
    participant log_path_write_text as log_path.write_text
    participant TestSessionIdResolverCaching
    participant SessionIdResolver_reset as SessionIdResolver.reset
    participant session_file_write_text as session_file.write_text
    participant resolver_get as resolver.get
    participant session_file_unlink as session_file.unlink
    participant resolver_invalidate as resolver.invalidate
    participant TestSessionIdResolverPriority
    participant primary_write_text as primary.write_text
    participant logs_dir_mkdir as logs_dir.mkdir
    participant fallback_write_text as fallback.write_text
    participant test_new_components
    participant TestCodeGraphAnalyzerImport
    participant TestCodeGraphAnalyzerInterface
    participant mod_CodeGraphAnalyzer as mod.CodeGraphAnalyzer
    participant TestCodeGraphAnalyzerGracefulFailure
    participant analyzer_run as analyzer.run
    participant TestCodeGraphAnalyzerScoringRange
    participant code_graph_analyzer as code-graph-analyzer
    participant TestSessionPrunerImport
    participant TestSessionPrunerEmptyDir
    participant session_pruner as session-pruner
    participant TestContextMonitorImport
    participant TestContextMonitorEmptySession
    participant context_monitor as context-monitor
    participant TestPatternDetectorImport
    participant TestPatternDetectorCurrentProject
    participant pattern_detector as pattern-detector
    participant TestPreferenceTrackerImport
    participant TestPreferenceTrackerEmpty
    participant preference_tracker as preference-tracker
    participant TestLevel1CheckReturnsTuple
    participant TestLevel2CheckReturnsTuple
    participant TestLevel1CheckNoTrace
    participant monkeypatch_setattr as monkeypatch.setattr
    participant TestLevel2CheckNoTrace
    participant test_post_tool_tracker
    participant progress_dir_mkdir as progress_dir.mkdir
    participant progress_file_write_text as progress_file.write_text
    participant TestIsErrorResponse
    participant TestEstimateContextPct
    participant TestGetResponseContentLength
    participant TestLoadSessionProgress
    participant TestSaveLoadSessionProgress
    participant nested_file_exists as nested_file.exists
    participant TestLogToolEntry
    participant tracker_log_read_text as tracker_log.read_text
    participant TestCheckLevel38PhaseRequirement
    participant TestCheckLevel39BuildValidation
    participant TestCheckLevel311GitStatus
    participant OSError
    participant TestDetectResultFailure
    participant mock_detector_detect_failure_in_message_assert_called_once as mock_detector.detect_failure_in_message.assert_called_once
    participant TestClearSessionFlags
    participant flag_dir_mkdir as flag_dir.mkdir
    participant flag_file_write_text as flag_file.write_text
    participant test_post_tool_tracker_mcp_server
    participant logs_path_mkdir as logs_path.mkdir
    participant TestTrackToolUsageBasic
    participant TestTrackToolUsageErrorHandling
    participant TestTrackToolUsageProgressDelta
    participant TestTrackToolUsageModifiedFiles
    participant TestTrackToolUsageWarnings
    participant TestTrackToolUsageToolLogger
    participant tracker_file_read_text as tracker_file.read_text
    participant sessions_dir_rglob as sessions_dir.rglob
    participant TestIncrementProgress
    participant TestClearEnforcementFlag
    participant flags_dir_mkdir as flags_dir.mkdir
    participant TestGetProgressStatus
    participant TestGetToolStats
    participant tracker_file_write_text as tracker_file.write_text
    participant TestCheckCommitReadiness
    participant TestToolCountsPersistence
    participant TestTaskUpdateFrequency
    participant TestJsonFormatConsistency
    participant test_pre_tool_enforcer
    participant data_update as data.update
    participant flag_name_lstrip as flag_name.lstrip
    participant TestCheckBash
    participant TestCheckPythonUnicode
    participant TestCheckGrep
    participant TestCheckRead
    participant large_file_write_text as large_file.write_text
    participant small_file_write_text as small_file.write_text
    participant TestCheckCheckpointPending
    participant TestCheckTaskBreakdownPending
    participant TestCheckSkillSelectionPending
    participant TestCheckLevel1SyncComplete
    participant TestCheckLevel2StandardsComplete
    participant TestCheckWriteEdit
    participant b_lower as b.lower
    participant TestLoadFlowTraceContext
    participant pte__load_flow_trace_context as pte._load_flow_trace_context
    participant TestFindSessionFlag
    participant old_time_isoformat as old_time.isoformat
    participant test_pre_tool_gate_mcp_server
    participant current_session_file_write_text as current_session_file.write_text
    participant created_at_isoformat as created_at.isoformat
    participant trace_file_write_text as trace_file.write_text
    participant TestValidateToolCallAlwaysAllowed
    participant TestValidateToolCallBlocked
    participant TestValidateToolCallBashWindowsBlocking
    participant TestValidateToolCallPythonUnicodeBlocking
    participant TestValidateToolCallHints
    participant TestCheckTaskBreakdown
    participant TestCheckSkillSelected
    participant TestCheckLevelCompletion
    participant TestGetEnforcerState
    participant TestCheckFailurePatterns
    participant h_lower as h.lower
    participant TestGetDynamicSkillHint
    participant TestResetEnforcerFlags
    participant legacy_flag_write_text as legacy_flag.write_text
    participant TestInternalHelpers
    participant _mod__get_session_id as _mod._get_session_id
    participant _mod__pipeline_step_present as _mod._pipeline_step_present
    participant _mod__json as _mod._json
    participant TestRAGLayerInit
    participant RAGLayer
    participant rag_get_stats as rag.get_stats
    participant TestRAGLookup
    participant TestRAGStore
    participant mock_client_upsert_assert_called_once as mock_client.upsert.assert_called_once
    participant TestConvenienceFunctions
    participant rag_integration
    participant TestSkillSelectionRAGBoost
    participant skill_selection_criteria
    participant TestRAGLookupInRunStep
    participant mock_rag_lookup_assert_called_once as mock_rag_lookup.assert_called_once
    participant dummy_fn_assert_called_once as dummy_fn.assert_called_once
    participant mock_rag_lookup_assert_not_called as mock_rag_lookup.assert_not_called
    participant dummy_fn_assert_not_called as dummy_fn.assert_not_called
    participant test_recovery_handler
    participant RecoveryHandler
    participant TestRecoveryHandlerInit
    participant TestInstallSignalHandlers
    participant mock_signal_signal_assert_not_called as mock_signal.signal.assert_not_called
    participant TestUpdateState
    participant mock_reg_assert_called_once_with as mock_reg.assert_called_once_with
    participant TestSaveStepCheckpoint
    participant handler_checkpoint_manager_save_checkpoint_assert_called_once_with as handler.checkpoint_manager.save_checkpoint.assert_called_once_with
    participant handler_error_logger_log_error_assert_called as handler.error_logger.log_error.assert_called
    participant TestIsTransientError
    participant recovery_handler
    participant SyntaxError
    participant TestBackoffDelay
    participant TestResumeSession
    participant TestResumeFromCheckpointPublicApi
    participant MockHandler_assert_called_once as MockHandler.assert_called_once
    participant mock_instance_resume_session_assert_called_once as mock_instance.resume_session.assert_called_once
    participant mock_instance_resume_session_assert_called_once_with as mock_instance.resume_session.assert_called_once_with
    participant TestRetryHistoryTracking
    participant flow_state
    participant TestRetryHistorySummary
    participant test_root_scripts
    participant TestSessionIDGenerator
    participant self_mod_SessionIDGenerator as self.mod.SessionIDGenerator
    participant gen_generate_session_id as gen.generate_session_id
    participant tid_startswith as tid.startswith
    participant TestMetricsEmitter
    participant self_metrics_file_exists as self.metrics_file.exists
    participant self_metrics_file_read_text as self.metrics_file.read_text
    participant self_mod_emit_hook_execution as self.mod.emit_hook_execution
    participant self_mod_emit_enforcement_event as self.mod.emit_enforcement_event
    participant TestPolicyTrackingHelper
    participant importlib_reload as importlib.reload
    participant self_pth_record_policy_execution as self.pth.record_policy_execution
    participant self_pth_record_sub_operation as self.pth.record_sub_operation
    participant self_pth_get_session_id as self.pth.get_session_id
    participant TestProjectSession
    participant session_file_parent_mkdir as session_file.parent.mkdir
    participant TestIdePaths
    participant TestRelease
    participant self_version_file_write_text as self.version_file.write_text
    participant self_mod_read_version as self.mod.read_version
    participant self_mod_bump_version as self.mod.bump_version
    participant TestPipelineBenchmark
    participant self_pb_PipelineBenchmark as self.pb.PipelineBenchmark
    participant bench_record_step as bench.record_step
    participant bench_get_summary as bench.get_summary
    participant PipelineBenchmark
    participant bench_save as bench.save
    participant path_obj_exists as path_obj.exists
    participant path_obj_read_text as path_obj.read_text
    participant TestMetricsDashboard
    participant self_md_MetricsDashboard as self.md.MetricsDashboard
    participant MetricsDashboard
    participant test_session_mcp_server
    participant sessions_mkdir as sessions.mkdir
    participant state_mkdir as state.mkdir
    participant TestSessionSave
    participant saved_exists as saved.exists
    participant session_mcp_server
    participant saved_read_text as saved.read_text
    participant TestSessionLoad
    participant proj_mkdir as proj.mkdir
    participant TestSessionList
    participant TestSessionArchive
    participant TestSessionQuery
    participant test_skill_management
    participant skills_root_mkdir as skills_root.mkdir
    participant SkillManager
    participant TestParseSkillMetadata
    participant dependency_resolver
    participant next
    participant TestDetectCircular
    participant issubset
    participant TestResolveDependencies
    participant resolved_index as resolved.index
    participant TestBuildDependencyGraph
    participant TestVersion
    participant TestCheckCompatibility
    participant version_selector
    participant TestSelectBestVersion
    participant TestHandleDeprecated
    participant TestValidateVersionSet
    participant TestBuildCompatibilityMatrix
    participant TestSkillManagerDownloadRetry
    participant url_attempts_items as url_attempts.items
    participant url_attempts_get as url_attempts.get
    participant TestSkillManagerCache
    participant mock_dl2_assert_not_called as mock_dl2.assert_not_called
    participant mock_dl_assert_not_called as mock_dl.assert_not_called
    participant mock_dl2_assert_called as mock_dl2.assert_called
    participant saved_file_exists as saved_file.exists
    participant saved_file_read_text as saved_file.read_text
    participant TestSkillManagerProvisionBatch
    participant TestSkillManagerValidateSet
    participant TestGetSkillManager
    participant skill_manager
    participant TestSkillManagementIntegration
    participant download_responses_items as download_responses.items
    participant test_skill_manager_mcp_server
    participant skill_file_write_text as skill_file.write_text
    participant agent_file_write_text as agent_file.write_text
    participant TestSkillLoadAll
    participant skills_dir_mkdir as skills_dir.mkdir
    participant skill_get as skill.get
    participant TestSkillLoad
    participant TestSkillSearch
    participant TestSkillValidate
    participant TestSkillRank
    participant TestSkillDetectConflicts
    participant TestAgentLoadAll
    participant agents_dir_mkdir as agents_dir.mkdir
    participant TestAgentLoad
    participant TestExtractMetadata
    participant TestJsonResponseFormat
    participant fn
    participant test_standards_ac
    participant STANDARDS_INTEGRATION_POINTS_items as STANDARDS_INTEGRATION_POINTS.items
    participant STANDARDS_INTEGRATION_POINTS_keys as STANDARDS_INTEGRATION_POINTS.keys
    participant markers_items as markers.items
    participant standards_schema
    participant schema_validate as schema.validate
    participant standard_selector
    participant graph_nodes_keys as graph.nodes.keys
    participant fpath_read_text as fpath.read_text
    participant content_encode as content.encode
    participant dict
    participant standards_integration
    participant test_standards_loader_mcp_server
    participant fpath_parent_mkdir as fpath.parent.mkdir
    participant fpath_write_text as fpath.write_text
    participant extra_files_items as extra_files.items
    participant fp_write_text as fp.write_text
    participant TestDetectFramework
    participant TestLoadStandards
    participant custom_dir_mkdir as custom_dir.mkdir
    participant TestResolveStandardConflicts
    participant TestGetActiveStandards
    participant TestListAvailableStandards
    participant TestExtractRules
    participant TestDetectConflictsHelper
    participant TestResolveConflictsHelper
    participant TestStep11CodeReview
    participant rec1_lower as rec1.lower
    participant rec2_lower as rec2.lower
    participant TestStep11Summary
    participant test_stop_notifier
    participant p_write_text as p.write_text
    participant content_str_encode as content_str.encode
    participant TestReadFlag
    participant p_exists as p.exists
    participant TestDeleteFlag
    participant TestIncrementRetry
    participant TestGetSessionStartDefault
    participant msg_strip as msg.strip
    participant TestGetWorkDoneDefault
    participant TestGetTaskCompleteDefault
    participant TestCompleteFlow
    participant mock_llm_assert_called_once_with as mock_llm.assert_called_once_with
    participant mock_speak_assert_called_once_with as mock_speak.assert_called_once_with
    participant mock_delete_assert_called_once_with as mock_delete.assert_called_once_with
    participant _mod_main as _mod.main
    participant mock_llm_assert_called_once as mock_llm.assert_called_once
    participant mock_speak_assert_called as mock_speak.assert_called
    participant mock_delete_assert_called_with as mock_delete.assert_called_with
    participant mock_speak_assert_not_called as mock_speak.assert_not_called
    participant mock_llm_assert_not_called as mock_llm.assert_not_called
    participant mock_speak_assert_called_once as mock_speak.assert_called_once
    participant TestPriorityOrder
    participant TestGenerateDynamicMessage
    participant self_api_key_file_write_text as self.api_key_file.write_text
    participant URLError
    participant TestLogging
    participant self_log_patcher_start as self.log_patcher.start
    participant self_log_patcher_stop as self.log_patcher.stop
    participant self_log_file_read_text as self.log_file.read_text
    participant self_log_file_exists as self.log_file.exists
    participant api_key_file_write_text as api_key_file.write_text
    participant TestSpeak
    participant self_voice_script_write_text as self.voice_script.write_text
    participant mock_popen_assert_not_called as mock_popen.assert_not_called
    participant mock_popen_assert_called_once as mock_popen.assert_called_once
    participant test_token_optimization_mcp_server
    participant path_parent_mkdir as path.parent.mkdir
    participant TestOptimizeToolCallRead
    participant optimized_get as optimized.get
    participant all_suggestions_lower as all_suggestions.lower
    participant TestOptimizeToolCallGrep
    participant TestOptimizeToolCallEdit
    participant target_write_text as target.write_text
    participant TestOptimizeToolCallGeneral
    participant TestAstNavigateCode
    participant txt_file_write_text as txt_file.write_text
    participant TestSmartReadAnalyze
    participant TestDeduplicateContext
    participant TestDedupEstimate
    participant TestContextBudgetStatus
    participant memory_dir_mkdir as memory_dir.mkdir
    participant test_uml_generators
    participant src_dir_mkdir as src_dir.mkdir
    participant sample_py_write_text as sample_py.write_text
    participant utils_py_write_text as utils_py.write_text
    participant services_dir_mkdir as services_dir.mkdir
    participant svc_py_write_text as svc_py.write_text
    participant UMLAstAnalyzer
    participant UMLDiagramGenerator
    participant TestUMLAstAnalyzer
    participant empty_write_text as empty.write_text
    participant bad_write_text as bad.write_text
    participant analyzer_build_dependency_graph as analyzer.build_dependency_graph
    participant TestUMLDiagramGenerator
    participant generator_generate_class_diagram as generator.generate_class_diagram
    participant syntax_startswith as syntax.startswith
    participant generator_generate_package_diagram as generator.generate_package_diagram
    participant generator_generate_component_diagram as generator.generate_component_diagram
    participant generator_generate_sequence_diagram as generator.generate_sequence_diagram
    participant TestKrokiRenderer
    participant uml_generators
    participant KrokiRenderer
    participant read_bytes
    participant TestUtilities
    participant result_startswith as result.startswith
    participant result_endswith as result.endswith
    participant gen_generate_class_diagram as gen.generate_class_diagram
    participant syntax_split as syntax.split
    participant py_file_write_text as py_file.write_text
    participant results_items as results.items
    participant uml_dir_exists as uml_dir.exists
    participant uml_dir_glob as uml_dir.glob
    participant TestCallGraphIntegration
    participant dep_graph_items as dep_graph.items
    participant c_get as c.get
    participant TestCallGraphDiagram
    participant src_write_text as src.write_text
    participant gen_generate_call_graph_diagram as gen.generate_call_graph_diagram
    participant _NullCG
    participant TestGenerateAllCompleteness
    participant TestSequenceDiagramFQN
    participant gen_generate_sequence_diagram as gen.generate_sequence_diagram
    participant test_vector_db_mcp_server
    participant TestVectorHealthCheck
    participant vector_db_mcp_server
    participant TestVectorCollectionStats
    participant TestVectorIndexToolCall
    participant TestVectorIndexSession
    participant TestVectorIndexFlowTrace
    participant TestVectorSearch
    participant TestVectorDeleteCollection
    participant mock_client_delete_collection_assert_called_once_with as mock_client.delete_collection.assert_called_once_with
    participant mock_client_create_collection_assert_called_once as mock_client.create_collection.assert_called_once
    participant TestVectorBulkIndex
    participant TestCollectionDefinitions
    participant _vec_mod_COLLECTIONS_items as _vec_mod.COLLECTIONS.items
    participant test_all_14_steps
    participant TestFlowStateSchema
    participant TestLevelMinus1Nodes
    participant ascii_file_write_text as ascii_file.write_text
    participant TestStep0TaskAnalysis
    participant TestStep1PlanModeDecision
    participant refined_get as refined.get
    participant TestStep5SkillAgentSelection
    participant TestStep7PromptGeneration
    participant OllamaService
    participant state_pop as state.pop
    participant TestStep8GithubIssue
    participant TestStep9BranchCreation
    participant branch_lower as branch.lower
    participant TestStep10Implementation
    participant TestStep11PullRequestReview
    participant TestSteps12to14Closure
    participant TestStepSequenceIntegration
    participant result4_get as result4.get
    participant TestV2StepWrapper
    participant TestCheckpointManagerIntegration
    participant cp_checkpoint_dir_mkdir as cp.checkpoint_dir.mkdir
    participant checkpoint_manager
    participant latest_file_exists as latest_file.exists
    participant latest_file_read_text as latest_file.read_text
    participant TestErrorLoggerIntegration
    participant audit_path_read_text as audit_path.read_text
    participant TestOrchestratorRouting
    participant TestEndToEndWorkflow
    participant test_failure_scenarios
    participant TestCallExecutionScriptFailures
    participant subprocess_TimeoutExpired as subprocess.TimeoutExpired
    participant TestNetworkFailureGithub
    participant TestLLMFailureOllamaOffline
    participant requests_ConnectionError as requests.ConnectionError
    participant service_chat as service.chat
    participant TestLLMMalformedResponse
    participant TestTimeoutHandling
    participant requests_Timeout as requests.Timeout
    participant TestFileSystemErrors
    participant PermissionError
    participant TestLevelMinus1RetryLoop
    participant non_ascii_file_write_bytes as non_ascii_file.write_bytes
    participant TestStep11ReviewRetryLoop
    participant TestCheckpointRecovery
    participant saved_state_update as saved_state.update
    participant recovered_state_get as recovered_state.get
    participant metadata_get as metadata.get
    participant bad_file_write_text as bad_file.write_text
    participant TestRecoveryHandlerSigint
    participant recovery_handler__current_state_get as recovery_handler._current_state.get
    participant TestHybridInferenceFailures
    participant HybridInferenceManager_STEP_ROUTING_items as HybridInferenceManager.STEP_ROUTING.items
    participant TestErrorLoggerEdgeCases
    participant TestMetricsCollectorDegradedMode
    participant TestWindowsPathHandling
    participant TestStatIsolation
    participant mcp_tool as mcp.tool
    participant _enforcer_store_save as _enforcer_store.save
    participant script_map_get as script_map.get
    participant required_steps_items as required_steps.items
    participant legacy_keys_items as legacy_keys.items
    participant step_data_get as step_data.get
    participant POLICIES_DIR_exists as POLICIES_DIR.exists
    participant level_dir_rglob as level_dir.rglob
    participant policy_file_relative_to as policy_file.relative_to
    participant policy_file_stat as policy_file.stat
    participant POLICIES_DIR_iterdir as POLICIES_DIR.iterdir
    participant title
    participant policy_file_stem_replace as policy_file.stem.replace
    participant flow_trace_file_with_suffix as flow_trace_file.with_suffix
    participant temp_write_text as temp.write_text
    participant temp_replace as temp.replace
    participant results_by_level_items as results_by_level.items
    participant mod_path_exists as mod_path.exists
    participant results_by_level_values as results_by_level.values
    participant fp_exists as fp.exists
    participant fp_stat as fp.stat
    participant _ilu_spec_from_file_location as _ilu.spec_from_file_location
    participant mcp_resource as mcp.resource
    participant vector_db_path_exists as vector_db_path.exists
    participant _json_mod_loads as _json_mod.loads
    participant vdb_result_get as vdb_result.get
    participant concurrent_futures_ThreadPoolExecutor as concurrent.futures.ThreadPoolExecutor
    participant concurrent_futures_as_completed as concurrent.futures.as_completed
    participant mod_result_get as mod_result.get
    participant mcp_result_get as mcp_result.get
    participant sqlite3_connect as sqlite3.connect
    participant Level3Step1Planner
    participant conn_close as conn.close
    participant _time_time as _time.time
    participant requests_get as requests.get
    participant executor_submit as executor.submit
    participant future_result as future.result
    participant v_get as v.get
    participant providers_status_values as providers_status.values
    participant requests_post as requests.post
    participant r_json as r.json
    participant memory_dir_rglob as memory_dir.rglob
    participant f_is_file as f.is_file
    participant db_path_stat as db_path.stat
    participant vector_db_path_rglob as vector_db_path.rglob
    participant cmd_extend as cmd.extend
    participant repo_create_issue as repo.create_issue
    participant issue_created_at_isoformat as issue.created_at.isoformat
    participant labels_split as labels.split
    participant repo_get_issue as repo.get_issue
    participant issue_edit as issue.edit
    participant issue_create_comment as issue.create_comment
    participant repo_get_pull as repo.get_pull
    participant pr_create_issue_comment as pr.create_issue_comment
    participant repo_create_pull as repo.create_pull
    participant pr_created_at_isoformat as pr.created_at.isoformat
    participant pr_add_to_labels as pr.add_to_labels
    participant repo_get_git_ref as repo.get_git_ref
    participant repo_get_issues as repo.get_issues
    participant repo_get_label as repo.get_label
    participant repo_get_commit as repo.get_commit
    participant commit_get_statuses as commit.get_statuses
    participant repo_is_dirty as repo.is_dirty
    participant repo_git_stash as repo.git.stash
    participant origin_fetch as origin.fetch
    participant repo_git_checkout as repo.git.checkout
    participant origin_push as origin.push
    participant _re_sub as _re.sub
    participant subject_lower as subject.lower
    participant repo_git_add as repo.git.add
    participant GitOperations
    participant gh_repo_create_pull as gh_repo.create_pull
    participant issue_add_to_labels as issue.add_to_labels
    participant repo_index_diff as repo.index.diff
    participant repo_git_branch as repo.git.branch
    participant repo_index_add as repo.index.add
    participant files_split as files.split
    participant origin_pull as origin.pull
    participant repo_git_diff as repo.git.diff
    participant args_extend as args.extend
    participant repo_iter_commits as repo.iter_commits
    participant commit_committed_datetime_isoformat as commit.committed_datetime.isoformat
    participant commit_message_strip as commit.message.strip
    participant repo_remote as repo.remote
    participant remote_obj_fetch as remote_obj.fetch
    participant response
    participant default_temps_get as default_temps.get
    participant target_call as target.call
    participant p_is_available as p.is_available
    participant urllib_request_Request as urllib.request.Request
    participant urllib_request_urlopen as urllib.request.urlopen
    participant results_values as results.values
    participant stat_result_stdout_strip as stat_result.stdout.strip
    participant title_lower as title.lower
    participant response_strip as response.strip
    participant _STEP_CLASSIFICATION_get as _STEP_CLASSIFICATION.get
    participant _MODEL_RECOMMENDATIONS_get as _MODEL_RECOMMENDATIONS.get
    participant model_dir_exists as model_dir.exists
    participant model_dir_rglob as model_dir.rglob
    participant response_text_strip as response_text.strip
    participant mcp_errors
    participant func
    participant _session_resolver_get as _session_resolver.get
    participant _progress_store_save as _progress_store.save
    participant params_get as params.get
    participant fd_get as fd.get
    participant trace_file_read_text as trace_file.read_text
    participant fp_replace as fp.replace
    participant fp_unlink as fp.unlink
    participant legacy_unlink as legacy.unlink
    participant by_tool_get as by_tool.get
    participant files_modified_add as files_modified.add
    participant legacy_read_text as legacy.read_text
    participant command_startswith as command.startswith
    participant p_is_file as p.is_file
    participant blocked_cmd_strip as blocked_cmd.strip
    participant skill_data_get as skill_data.get
    participant translations_items as translations.items
    participant kb_path_read_text as kb_path.read_text
    participant _EXT_SKILL_MAP_get as _EXT_SKILL_MAP.get
    participant name_endswith as name.endswith
    participant SESSIONS_PATH_mkdir as SESSIONS_PATH.mkdir
    participant STATE_PATH_mkdir as STATE_PATH.mkdir
    participant session_path_with_suffix as session_path.with_suffix
    participant session_path_read_text as session_path.read_text
    participant tags_add as tags.add
    participant rstrip
    participant project_cwd_replace as project_cwd.replace
    participant file_path_with_suffix as file_path.with_suffix
    participant temp_path_replace as temp_path.replace
    participant file_path_stat as file_path.stat
    participant file_path_exists as file_path.exists
    participant project_dir_exists as project_dir.exists
    participant sessions_sort as sessions.sort
    participant SESSIONS_PATH_exists as SESSIONS_PATH.exists
    participant proj_dir_exists as proj_dir.exists
    participant proj_dir_glob as proj_dir.glob
    participant SESSIONS_PATH_iterdir as SESSIONS_PATH.iterdir
    participant session_file_stem_replace as session_file.stem.replace
    participant archive_dir_mkdir as archive_dir.mkdir
    participant proj_dir_name_startswith as proj_dir.name.startswith
    participant proj_dir_is_dir as proj_dir.is_dir
    participant dest_dir_mkdir as dest_dir.mkdir
    participant shutil_move as shutil.move
    participant filter_dict_get as filter_dict.get
    participant file_date_isoformat as file_date.isoformat
    participant d_name_startswith as d.name.startswith
    participant _chain_store_save as _chain_store.save
    participant CURRENT_SESSION_FILE_parent_mkdir as CURRENT_SESSION_FILE.parent.mkdir
    participant CURRENT_SESSION_FILE_write_text as CURRENT_SESSION_FILE.write_text
    participant now_isoformat as now.isoformat
    participant tags_split as tags.split
    participant other_session_get as other_session.get
    participant session_setdefault as session.setdefault
    participant other_session_setdefault as other_session.setdefault
    participant parent_get as parent.get
    participant matching_values as matching.values
    participant index_get as index.get
    participant tool_tracker_file_exists as tool_tracker_file.exists
    participant divmod
    participant diff_total_seconds as diff.total_seconds
    participant tool_tracker_file_read_text as tool_tracker_file.read_text
    participant files_read_add as files_read.add
    participant re_findall as re.findall
    participant c_strip as c.strip
    participant line_s_startswith as line_s.startswith
    participant line_s_split as line_s.split
    participant val_strip as val.strip
    participant fm_match_group as fm_match.group
    participant key_strip as key.strip
    participant val_lower as val.lower
    participant val_split as val.split
    participant SKILLS_DIR_glob as SKILLS_DIR.glob
    participant fp_read_text as fp.read_text
    participant SKILLS_DIR_exists as SKILLS_DIR.exists
    participant SKILLS_DIR_rglob as SKILLS_DIR.rglob
    participant skill_file_read_text as skill_file.read_text
    participant skill_file_stat as skill_file.stat
    participant query_lower as query.lower
    participant results_sort as results.sort
    participant project_type_lower as project_type.lower
    participant c_lower as c.lower
    participant required_capabilities_split as required_capabilities.split
    participant ranked_sort as ranked.sort
    participant n_strip as n.strip
    participant skill_names_split as skill_names.split
    participant s1_get as s1.get
    participant s2_get as s2.get
    participant n2_lower as n2.lower
    participant n1_lower as n1.lower
    participant AGENTS_DIR_exists as AGENTS_DIR.exists
    participant AGENTS_DIR_iterdir as AGENTS_DIR.iterdir
    participant agent_dir_is_dir as agent_dir.is_dir
    participant agent_file_exists as agent_file.exists
    participant agent_file_read_text as agent_file.read_text
    participant agent_file_stat as agent_file.stat
    participant root_exists as root.exists
    participant root_glob as root.glob
    participant pkg_exists as pkg.exists
    participant pkg_read_text as pkg.read_text
    participant pt_result_get as pt_result.get
    participant fw_result_get as fw_result.get
    participant all_standards_extend as all_standards.extend
    participant d_rglob as d.rglob
    participant md_file_name_lower as md_file.name.lower
    participant md_file_read_text as md_file.read_text
    participant md_file_stat as md_file.stat
    participant ARCH_STANDARDS_DIR_glob as ARCH_STANDARDS_DIR.glob
    participant policy_standards_exists as policy_standards.exists
    participant policy_standards_rglob as policy_standards.rglob
    participant re_finditer as re.finditer
    participant std1_get as std1.get
    participant std2_get as std2.get
    participant rules1_keys as rules1.keys
    participant rules2_keys as rules2.keys
    participant std_get as std.get
    participant rules_items as rules.items
    participant ARCH_STANDARDS_DIR_exists as ARCH_STANDARDS_DIR.exists
    participant policy_dir_exists as policy_dir.exists
    participant ARCH_STANDARDS_DIR_rglob as ARCH_STANDARDS_DIR.rglob
    participant policy_dir_rglob as policy_dir.rglob
    participant f_name_lower as f.name.lower
    participant Observer
    participant StandardsChangeHandler
    participant observer_start as observer.start
    participant observer_schedule as observer.schedule
    participant event_src_path_endswith as event.src_path.endswith
    participant tool_name_lower as tool_name.lower
    participant _file_access_count_get as _file_access_count.get
    participant path_is_file as path.is_file
    participant path_suffix_lower as path.suffix.lower
    participant p_strip as p.strip
    participant command_split as command.split
    participant path_parent_exists as path.parent.exists
    participant package_group as package.group
    participant ast_module_walk as ast_module.walk
    participant imports_extend as imports.extend
    participant hashlib_md5 as hashlib.md5
    participant text_encode as text.encode
    participant deduped_items as deduped.items
    participant t_encode as t.encode
    participant texts_values as texts.values
    participant seen_fps_add as seen_fps.add
    participant line_encode as line.encode
    participant MEMORY_PATH_exists as MEMORY_PATH.exists
    participant MEMORY_PATH_rglob as MEMORY_PATH.rglob
    participant OPTIMIZATION_LOG_exists as OPTIMIZATION_LOG.exists
    participant OPTIMIZATION_LOG_read_text as OPTIMIZATION_LOG.read_text
    participant LOGS_PATH_mkdir as LOGS_PATH.mkdir
    participant uml_diagram_mcp_server
    participant function_path_rsplit as function_path.rsplit
    participant file_full_is_file as file_full.is_file
    participant file_full_read_text as file_full.read_text
    participant time_time_ns as time.time_ns
    participant ts_encode as ts.encode
    participant EmbeddingManager
    participant PointStruct
    participant client_upsert as client.upsert
    participant client_search as client.search
    participant Filter
    participant FieldCondition
    participant MatchValue
    participant COLLECTIONS_keys as COLLECTIONS.keys
    participant client_get_collection as client.get_collection
    participant db_path_rglob as db_path.rglob
    participant client_delete_collection as client.delete_collection
    participant client_create_collection as client.create_collection
    participant VectorParams
    participant embedding_mgr_get as embedding_mgr.get
    participant record_pop as record.pop
    participant ClaudeCredentialsManager
    participant PathResolver
    participant self_config_dir_mkdir as self.config_dir.mkdir
    participant self_key_file_exists as self.key_file.exists
    participant Fernet_generate_key as Fernet.generate_key
    participant os_chmod as os.chmod
    participant Fernet
    participant self_cipher_encrypt as self.cipher.encrypt
    participant self_credentials_file_exists as self.credentials_file.exists
    participant self_cipher_decrypt as self.cipher.decrypt
    participant credentials_get as credentials.get
    participant decrypted_data_decode as decrypted_data.decode
    participant self_credentials_file_unlink as self.credentials_file.unlink
    participant ClaudeAPIClient
    participant AutoSessionTracker
    participant self_auto_tracking_config_exists as self.auto_tracking_config.exists
    participant ImportManager
    participant response_read as response.read
    participant module_path_split as module_path.split
    participant path_resolver
    participant self_global_memory_exists as self.global_memory.exists
    participant self_base_dir_joinpath as self.base_dir.joinpath
    participant threading_Lock as threading.Lock
    participant cls
    participant cls__instances_values as cls._instances.values
    participant cls__instances_clear as cls._instances.clear
    participant Repo
    participant self__client_is_dirty as self._client.is_dirty
    participant Github
    participant url_startswith as url.startswith
    participant url_split as url.split
    participant url_rstrip as url.rstrip
    participant QdrantManager
    participant db_path_mkdir as db_path.mkdir
    participant QdrantClient
    participant self_COLLECTIONS_items as self.COLLECTIONS.items
    participant client_get_collections as client.get_collections
    participant self__client_get_collection as self._client.get_collection
    participant SentenceTransformer
    participant model_encode as model.encode
    participant embedding_tolist as embedding.tolist
    participant functools_wraps as functools.wraps
    participant decorator
    participant self__path_with_suffix as self._path.with_suffix
    participant self__path_parent_mkdir as self._path.parent.mkdir
    participant self__path_unlink as self._path.unlink
    participant filters_items as filters.items
    participant self__payload_update as self._payload.update
    participant payload_update as payload.update
    participant ComputerUseAgent
    participant self_screenshots_dir_mkdir as self.screenshots_dir.mkdir
    participant anthropic_Anthropic as anthropic.Anthropic
    participant mss_mss as mss.mss
    participant sct_grab as sct.grab
    participant mss_tools_to_png as mss.tools.to_png
    participant base64_b64encode as base64.b64encode
    participant pyautogui_write as pyautogui.write
    participant pyautogui_press as pyautogui.press
    participant computer_use_agent as computer-use-agent
    participant pyautogui_scroll as pyautogui.scroll
    participant pyautogui_hotkey as pyautogui.hotkey
    participant exit
    participant DummyProjectSeeder
    participant base_time_isoformat as base_time.isoformat
    participant end_time_isoformat as end_time.isoformat
    participant random_randint as random.randint
    participant timestamp_isoformat as timestamp.isoformat
    participant policy_name_lower as policy_name.lower
    participant dummy_project_seeder as dummy-project-seeder
    participant PreFlightChecker
    participant session_dir_is_dir as session_dir.is_dir
    participant policy_log_exists as policy_log.exists
    participant policy_log_stat as policy_log.stat
    participant status_code_startswith as status_code.startswith
    participant flow_trace_stat as flow_trace.stat
    participant mtime_strftime as mtime.strftime
    participant verify_computer_use_prerequisites as verify-computer-use-prerequisites
    participant self_backup_dir_mkdir as self.backup_dir.mkdir
    participant self_diff_dir_mkdir as self.diff_dir.mkdir
    participant expanduser
    participant file_path_name_replace as file_path.name.replace
    participant backup_file_write_bytes as backup_file.write_bytes
    participant file_path_read_bytes as file_path.read_bytes
    participant file_path_write_bytes as file_path.write_bytes
    participant backup_file_exists as backup_file.exists
    participant backup_file_read_bytes as backup_file.read_bytes
    participant difflib_unified_diff as difflib.unified_diff
    participant diff_file_write_text as diff_file.write_text
    participant backup_file_read_text as backup_file.read_text
    participant file_path_read_text as file_path.read_text
    participant content_strip as content.strip
    participant compile
    participant self_metadata_file_exists as self.metadata_file.exists
    participant self_metadata_file_read_text as self.metadata_file.read_text
    participant self_metadata_file_write_text as self.metadata_file.write_text
    participant backup_manager
    participant builders
    participant commit_type_strip as commit_type.strip
    participant scope_strip as scope.strip
    participant subject_strip as subject.strip
    participant body_strip as body.strip
    participant footer_strip as footer.strip
    participant PRBodyBuilder
    participant summary_strip as summary.strip
    participant label_strip as label.strip
    participant notes_strip as notes.strip
    participant note_strip as note.strip
    participant IssueBodyBuilder
    participant description_strip as description.strip
    participant plan_strip as plan.strip
    participant criterion_strip as criterion.strip
    participant data_encode as data.encode
    participant threading_RLock as threading.RLock
    participant self__store_values as self._store.values
    participant self__disk_dir_mkdir as self._disk_dir.mkdir
    participant logger
    participant logger_debug as logger.debug
    participant self__disk_dir_glob as self._disk_dir.glob
    participant f_read_text as f.read_text
    participant f_unlink as f.unlink
    participant p_resolve as p.resolve
    participant p_unlink as p.unlink
    participant call_fn
    participant read_fn
    participant load_fn
    participant importlib_import_module as importlib.import_module
    participant method_get as method.get
    participant p_relative_to as p.relative_to
    participant normalized_add as normalized.add
    participant affected_modules_add as affected_modules.add
    participant py_file_read_text as py_file.read_text
    participant name_startswith as name.startswith
    participant py_file_relative_to as py_file.relative_to
    participant defaultdict
    participant counter_items as counter.items
    participant edge_get as edge.get
    participant result_update as result.update
    participant affected_methods_sort as affected_methods.sort
    participant safe_change_zones_sort as safe_change_zones.sort
    participant danger_zones_sort as danger_zones.sort
    participant impact_map_get as impact_map.get
    participant logger_error as logger.error
    participant graph_methods_keys as graph.methods.keys
    participant g_stats_get as g_stats.get
    participant all_callees_add as all_callees.add
    participant cross_file_deps_raw_items as cross_file_deps_raw.items
    participant orphaned_methods_sort as orphaned_methods.sort
    participant breaking_changes_sort as breaking_changes.sort
    participant pre_change_snapshot_get as pre_change_snapshot.get
    participant nodes_get as nodes.get
    participant pre_methods_get as pre_methods.get
    participant pre_method_get as pre_method.get
    participant current_edge_keys_add as current_edge_keys.add
    participant current_callers_get as current_callers.get
    participant pre_edge_keys_add as pre_edge_keys.add
    participant pre_methods_values as pre_methods.values
    participant _CallGraphVisitor
    participant self__class_stack_pop as self._class_stack.pop
    participant self__func_stack_pop as self._func_stack.pop
    participant self_edges_extend as self.edges.extend
    participant self_files_add as self.files.add
    participant self_methods_items as self.methods.items
    participant self_classes_items as self.classes.items
    participant target_rsplit as target.rsplit
    participant caller_fqn_split as caller_fqn.split
    participant c_startswith as c.startswith
    participant adjacency_values as adjacency.values
    participant all_callees_update as all_callees.update
    participant stack_pop as stack.pop
    participant adjacency_get as adjacency.get
    participant self_methods_get as self.methods.get
    participant reverse_get as reverse.get
    participant affected_add as affected.add
    participant self_methods_values as self.methods.values
    participant self_classes_values as self.classes.values
    participant CallGraphBuilder
    participant self_project_root_rglob as self.project_root.rglob
    participant py_file_stat as py_file.stat
    participant rel_path_replace as rel_path.replace
    participant visitor_visit as visitor.visit
    participant impact_get as impact.get
    participant _SqliteSaverClass_from_conn_string as _SqliteSaverClass.from_conn_string
    participant _SqliteSaverClass
    participant db_path_parent_mkdir as db_path.parent.mkdir
    participant conn_cursor as conn.cursor
    participant cursor_fetchone as cursor.fetchone
    participant state_items as state.items
    participant self_checkpoint_dir_mkdir as self.checkpoint_dir.mkdir
    participant os_replace as os.replace
    participant os_fdopen as os.fdopen
    participant step_part_startswith as step_part.startswith
    participant checkpoint_id_split as checkpoint_id.split
    participant self_checkpoint_dir_glob as self.checkpoint_dir.glob
    participant last_file_stem_split as last_file.stem.split
    participant f_stem_split as f.stem.split
    participant path_rglob as path.rglob
    participant ln_strip as ln.strip
    participant req_txt_exists as req_txt.exists
    participant pkg_json_exists as pkg_json.exists
    participant pyproject_exists as pyproject.exists
    participant pyproject_read_text as pyproject.read_text
    participant pkg_json_read_text as pkg_json.read_text
    participant req_txt_read_text as req_txt.read_text
    participant stripped_startswith as stripped.startswith
    participant pf_read_text as pf.read_text
    participant local_graph_cache_exists as local_graph_cache.exists
    participant analyzer_save as analyzer.save
    participant analyzer_module_exists as analyzer_module.exists
    participant local_graph_cache_parent_mkdir as local_graph_cache.parent.mkdir
    participant local_graph_cache_write_text as local_graph_cache.write_text
    participant call_stack_get as call_stack.get
    participant local_graph_cache_read_text as local_graph_cache.read_text
    participant cached_get as cached.get
    participant conflict_resolver
    participant removed_names_extend as removed_names.extend
    participant setting_owners_items as setting_owners.items
    participant STANDARD_PRIORITY_get as STANDARD_PRIORITY.get
    participant settings_items as settings.items
    participant conflicted_std_names_add as conflicted_std_names.add
    participant loser_std_get as loser_std.get
    participant winner_std_get as winner_std.get
    participant self_session_dir_mkdir as self.session_dir.mkdir
    participant skill_names_intersection as skill_names.intersection
    participant SKILL_DOMAIN_PRIORITY_get as SKILL_DOMAIN_PRIORITY.get
    participant loser_get as loser.get
    participant peer_get as peer.get
    participant name_to_skill_keys as name_to_skill.keys
    participant skill_a_get as skill_a.get
    participant skill_b_get as skill_b.get
    participant domain_map_items as domain_map.items
    participant to_keep_extend as to_keep.extend
    participant domain_map_setdefault as domain_map.setdefault
    participant winner_get as winner.get
    participant cap_lower as cap.lower
    participant existing_get as existing.get
    participant proc_stdout_strip as proc.stdout.strip
    participant proc_stderr_strip as proc.stderr.strip
    participant stdout_strip as stdout.strip
    participant self__miss_reasons_get as self._miss_reasons.get
    participant stats_file_write_text as stats_file.write_text
    participant self_cache_dir_mkdir as self.cache_dir.mkdir
    participant cache_file_exists as cache_file.exists
    participant age_total_seconds as age.total_seconds
    participant current_sigs_get as current_sigs.get
    participant cached_sigs_get as cached_sigs.get
    participant cache_file_unlink as cache_file.unlink
    participant match_stat as match.stat
    participant contexts_get as contexts.get
    participant original_text_splitlines as original_text.splitlines
    participant deduped_texts_items as deduped_texts.items
    participant seen_fingerprints_add as seen_fingerprints.add
    participant deduped_texts_values as deduped_texts.values
    participant DecisionExplanation
    participant DecisionExplainer
    participant decision_explainer
    participant capability_scores_get as capability_scores.get
    participant confidence_map_get as confidence_map.get
    participant alt_get as alt.get
    participant DeepSeekReasoning
    participant Anthropic
    participant deepseek_reasoning
    participant t_get as t.get
    participant self_ollama_service_call as self.ollama_service.call
    participant response_get as response.get
    participant ToonObject
    participant json_match_group as json_match.group
    participant _MANDATORY_SECTION_PATTERN_search as _MANDATORY_SECTION_PATTERN.search
    participant _OPTIONAL_SECTION_PATTERN_search as _OPTIONAL_SECTION_PATTERN.search
    participant _DEP_SECTION_PATTERN_search as _DEP_SECTION_PATTERN.search
    participant mandatory_match_group as mandatory_match.group
    participant optional_match_group as optional_match.group
    participant dep_match_group as dep_match.group
    participant text_splitlines as text.splitlines
    participant skill_match_group as skill_match.group
    participant ver_match_group as ver_match.group
    participant plain_match_group as plain_match.group
    participant rec_stack_add as rec_stack.add
    participant dependency_graph_get as dependency_graph.get
    participant path_pop as path.pop
    participant rec_stack_discard as rec_stack.discard
    participant dependency_graph_keys as dependency_graph.keys
    participant _dfs
    participant path_index as path.index
    participant frozenset
    participant skill_contents_get as skill_contents.get
    participant queue_insert as queue.insert
    participant reachable_add as reachable.add
    participant graph_get as graph.get
    participant dependents_get as dependents.get
    participant graph_values as graph.values
    participant CodebaseAnalyzer
    participant logging_getLogger as logging.getLogger
    participant documentation_generator
    participant extensions_map_items as extensions_map.items
    participant self_root_rglob as self.root.rglob
    participant languages_add as languages.add
    participant checks_items as checks.items
    participant indicator_split as indicator.split
    participant frameworks_add as frameworks.add
    participant folder_purposes_items as folder_purposes.items
    participant package_json_exists as package_json.exists
    participant req_file_exists as req_file.exists
    participant pkg_file_exists as pkg_file.exists
    participant deps_extend as deps.extend
    participant keys
    participant setup_file_exists as setup_file.exists
    participant vf_path_exists as vf_path.exists
    participant setup_file_read_text as setup_file.read_text
    participant vf_path_read_text as vf_path.read_text
    participant structure_items as structure.items
    participant DocumentationGenerator
    participant self_logger_error as self.logger.error
    participant update_func
    participant file_path_write_text as file_path.write_text
    participant context_test_command_split as context.test_command.split
    participant comp_get as comp.get
    participant category_lower as category.lower
    participant self_log_dir_mkdir as self.log_dir.mkdir
    participant operation_upper as operation.upper
    participant by_severity_get as by_severity.get
    participant self_audit_file_write_text as self.audit_file.write_text
    participant severity_symbols_get as severity_symbols.get
    participant error_logger
    participant FormattedError
    participant _ErrorCatalog
    participant error_type_upper as error_type.upper
    participant self__templates_items as self._templates.items
    participant key_upper as key.upper
    participant name_upper as name.upper
    participant error_messages
    participant model_split as model.split
    participant template_fn
    participant msg_context_update as msg.context.update
    participant topic_lower as topic.lower
    participant WorkflowEngineError
    participant StepExecutionError
    participant obj_get as obj.get
    participant toon_obj_get as toon_obj.get
    participant WorkflowContextOptimizer
    participant current_step_get as current_step.get
    participant memory_get as memory.get
    participant synthesis_file_parent_mkdir as synthesis_file.parent.mkdir
    participant synthesis_file_write_text as synthesis_file.write_text
    participant github_code_review
    participant diff_text_lower as diff_text.lower
    participant l_startswith as l.startswith
    participant text_find as text.find
    participant text_rfind as text.rfind
    participant fmt
    participant diff_text_split as diff_text.split
    participant i_startswith as i.startswith
    participant github_router_add_pr_comment as github_router.add_pr_comment
    participant git_ops_get_pr_diff as git_ops.get_pr_diff
    participant issues_extend as issues.extend
    participant eval_result_to_dict as eval_result.to_dict
    participant i_get as i.get
    participant GitHubFacade
    participant github_facade
    participant raw_get as raw.get
    participant self__router_create_pull_request as self._router.create_pull_request
    participant self__router_merge_pull_request as self._router.merge_pull_request
    participant remote_url_startswith as remote_url.startswith
    participant remote_url_split as remote_url.split
    participant remote_url_rstrip as remote_url.rstrip
    participant issue_url_rstrip as issue_url.rstrip
    participant comment_result_get as comment_result.get
    participant pr_url_rstrip as pr_url.rstrip
    participant self_github_get_user as self.github.get_user
    participant self_repo_get_issue as self.repo.get_issue
    participant self_repo_create_pull as self.repo.create_pull
    participant self_repo_get_pull as self.repo.get_pull
    participant self_repo_get_git_ref as self.repo.get_git_ref
    participant github_merge_validation
    participant git_ops__run_git as git_ops._run_git
    participant status_result_get as status_result.get
    participant indicators_items as indicators.items
    participant github_router_repo_get_pull as github_router.repo.get_pull
    participant merge_test_get as merge_test.get
    participant validation_get as validation.get
    participant self_repo_path_mkdir as self.repo_path.mkdir
    participant fetch_result_get as fetch_result.get
    participant pop_result_get as pop_result.get
    participant stash_result_get as stash_result.get
    participant del_result_get as del_result.get
    participant stage_result_get as stage_result.get
    participant hash_result_get as hash_result.get
    participant cmd_insert as cmd.insert
    participant branch_result_get as branch_result.get
    participant commit_result_get as commit_result.get
    participant push_result_get as push_result.get
    participant hooks_decorator
    participant wraps
    participant node_fn
    participant pre_hook
    participant post_hook
    participant main_hook
    participant routing_info_get as routing_info.get
    participant self_router_ollama_chat as self.router.ollama.chat
    participant temp_file_write as temp_file.write
    participant json_response_get as json_response.get
    participant json_out_get as json_out.get
    participant cli_result_get as cli_result.get
    participant hybrid_inference
    participant inference_config
    participant joinpath
    participant InferenceConfig
    participant gpu_exe_exists as gpu_exe.exists
    participant gpu_models_exists as gpu_models.exists
    participant npu_cli_exists as npu_cli.exists
    participant npu_models_exists as npu_models.exists
    participant mode_lower as mode.lower
    participant npu_service
    participant task_routing_get as task_routing.get
    participant inference_router
    participant level3_code_explorer
    participant file_path_is_file as file_path.is_file
    participant pattern_lower as pattern.lower
    participant line_lower as line.lower
    participant file_path_relative_to as file_path.relative_to
    participant _search
    participant key_files_items as key_files.items
    participant parallel_executor
    participant _grep
    participant user_requirement_lower as user_requirement.lower
    participant _read
    participant root_path_exists as root_path.exists
    participant root_path_iterdir as root_path.iterdir
    participant item_is_dir as item.is_dir
    participant item_name_startswith as item.name.startswith
    participant item_glob as item.glob
    participant root_path_rglob as root_path.rglob
    participant requirement_lower as requirement.lower
    participant file_path_name_lower as file_path.name.lower
    participant language_map_items as language_map.items
    participant extensions_get as extensions.get
    participant extend
    participant as_posix
    participant f_relative_to as f.relative_to
    participant full_path_is_file as full_path.is_file
    participant is_file
    participant key_mappings_items as key_mappings.items
    participant uml_result_items as uml_result.items
    participant created_extend as created.extend
    participant claude_md_path_is_file as claude_md_path.is_file
    participant readme_path_is_file as readme_path.is_file
    participant updated_files_extend as updated_files.extend
    participant srs_path_is_file as srs_path.is_file
    participant method_args
    participant uml_gen_generate_class_diagram as uml_gen.generate_class_diagram
    participant uml_gen_generate_package_diagram as uml_gen.generate_package_diagram
    participant uml_gen_generate_component_diagram as uml_gen.generate_component_diagram
    participant uml_gen_generate_sequence_diagram as uml_gen.generate_sequence_diagram
    participant uml_gen_generate_call_graph_diagram as uml_gen.generate_call_graph_diagram
    participant existing_split as existing.split
    participant requirements_get as requirements.get
    participant fpath_is_file as fpath.is_file
    participant level3_llm_retry
    participant __name___lower as __name__.lower
    participant level3_remaining_steps
    participant SessionManager
    participant plan_convergence
    participant best_plan_dict_get as best_plan_dict.get
    participant TokenBudget_estimate_tokens as TokenBudget.estimate_tokens
    participant self_inference_chat as self.inference.chat
    participant TokenBudget
    participant resp_get as resp.get
    participant _cache_llm_get as _cache.llm.get
    participant _cache_llm_set as _cache.llm.set
    participant plan_text_split as plan_text.split
    participant word_strip as word.strip
    participant section_split as section.split
    participant section_strip as section.strip
    participant plan_text_lower as plan_text.lower
    participant task_validator
    participant task_get as task.get
    participant plan_get as plan.get
    participant toon_analysis_get as toon_analysis.get
    participant toon_models
    participant risks_data_get as risks_data.get
    participant validated_model_dump as validated.model_dump
    participant uuid4
    participant f_endswith as f.endswith
    participant type_scores_values as type_scores.values
    participant patterns_items as patterns.items
    participant indicator_lower as indicator.lower
    participant skills_dir_exists as skills_dir.exists
    participant skills_dir_iterdir as skills_dir.iterdir
    participant category_dir_iterdir as category_dir.iterdir
    participant category_dir_is_dir as category_dir.is_dir
    participant skill_dir_is_dir as skill_dir.is_dir
    participant skill_file_exists as skill_file.exists
    participant skill_file_alt_exists as skill_file_alt.exists
    participant file_to_read_read_text as file_to_read.read_text
    participant agents_dir_exists as agents_dir.exists
    participant agents_dir_iterdir as agents_dir.iterdir
    participant urllib_request_urlretrieve as urllib.request.urlretrieve
    participant llm_recommendation_get as llm_recommendation.get
    participant skill_path_exists as skill_path.exists
    participant skill_path_alt_exists as skill_path_alt.exists
    participant platform_system as platform.system
    participant level3_step1_planner
    participant _time_sleep as _time.sleep
    participant ollama_service
    participant step_validator
    participant StepValidator
    participant self_ollama_step1_plan_mode_decision as self.ollama.step1_plan_mode_decision
    participant plan_decision_rules
    participant claude_client_step1_plan_mode_decision as claude_client.step1_plan_mode_decision
    participant step1_result_get as step1_result.get
    participant timeout_wrapper
    participant level3_steps8to12_github
    participant self_facade_create_issue as self.facade.create_issue
    participant self_ollama_chat as self.ollama.chat
    participant description_lower as description.lower
    participant builder_build as builder.build
    participant self_facade_create_branch as self.facade.create_branch
    participant branch_resolution_get as branch_resolution.get
    participant review_result_get as review_result.get
    participant self_github_add_pr_comment as self.github.add_pr_comment
    participant conflict_check_get as conflict_check.get
    participant self_facade_close_issue as self.facade.close_issue
    participant parts_extend as parts.extend
    participant step8_get as step8.get
    participant step9_get as step9.get
    participant step11_get as step11.get
    participant step12_get as step12.get
    participant self_github_repo_get_pull as self.github.repo.get_pull
    participant OllamaProvider
    participant urlparse
    participant _log_debug as _log.debug
    participant ClaudeCLIProvider
    participant shutil_which as shutil.which
    participant self_MODEL_MAP_get as self.MODEL_MAP.get
    participant OpenAIProvider
    participant AnthropicProvider
    participant _PROVIDER_REGISTRY_get as _PROVIDER_REGISTRY.get
    participant order_extend as order.extend
    participant LLMProvider
    participant fallback_str_split as fallback_str.split
    participant DEFAULT_TEMPERATURES_get as DEFAULT_TEMPERATURES.get
    participant LoggingSetup
    participant Console
    participant logger_remove as logger.remove
    participant logger_add as logger.add
    participant RichHandler
    participant logging_basicConfig as logging.basicConfig
    participant logging_root_removeHandler as logging.root.removeHandler
    participant Progress
    participant SpinnerColumn
    participant TextColumn
    participant BarColumn
    participant TimeRemainingColumn
    participant ExecutionLogger
    participant status_upper as status.upper
    participant logging_setup
    participant PluginManifest
    participant mcp_plugin_loader
    participant self_data_get as self.data.get
    participant MCPPluginLoader
    participant SimpleLogger
    participant self_plugins_path_exists as self.plugins_path.exists
    participant self_plugins_path_iterdir as self.plugins_path.iterdir
    participant self__logger_error as self._logger.error
    participant plugin_dir_is_dir as plugin_dir.is_dir
    participant manifest_path_exists as manifest_path.exists
    participant self__logger_debug as self._logger.debug
    participant manifest_path_read_text as manifest_path.read_text
    participant module_path_exists as module_path.exists
    participant plugin_class
    participant sys_path_remove as sys.path.remove
    participant plugins_items as plugins.items
    participant self__cache_clear as self._cache.clear
    participant operation_fn
    participant entry_update as entry.update
    participant self__step_metrics_setdefault as self._step_metrics.setdefault
    participant step_entry_setdefault as step_entry.setdefault
    participant self__all_files_modified_add as self._all_files_modified.add
    participant all_files_discard as all_files.discard
    participant self__step_metrics_values as self._step_metrics.values
    participant all_files_add as all_files.add
    participant fop_get as fop.get
    participant k_split as k.split
    participant self_metrics_file_write_text as self.metrics_file.write_text
    participant metrics_collector
    participant self_benchmark_dir_is_dir as self.benchmark_dir.is_dir
    participant self_benchmark_dir_glob as self.benchmark_dir.glob
    participant b_get as b.get
    participant steps_items as steps.items
    participant step_stats_items as step_stats.items
    participant step_stats_values as step_stats.values
    participant error_dist_get as error_dist.get
    participant self_output_dir_mkdir as self.output_dir.mkdir
    participant metrics_get as metrics.get
    participant errors_items as errors.items
    participant metrics_dashboard
    participant ModelDiscovery
    participant npu_path_glob as npu_path.glob
    participant npu_path_exists as npu_path.exists
    participant model_discovery
    participant openvino_path_iterdir as openvino_path.iterdir
    participant openvino_path_exists as openvino_path.exists
    participant model_dir_is_dir as model_dir.is_dir
    participant name_split as name.split
    participant gpu_path_exists as gpu_path.exists
    participant self_models_values as self.models.values
    participant platform_priority_get as platform_priority.get
    participant m_size_replace as m.size.replace
    participant ModelInfo
    participant self_models_items as self.models.items
    participant NPUService
    participant self_available_categories_items as self.available_categories.items
    participant self_cli_exe_exists as self.cli_exe.exists
    participant self_models_path_exists as self.models_path.exists
    participant self_available_categories_keys as self.available_categories.keys
    participant self_models_path_glob as self.models_path.glob
    participant model_path_name_lower as model_path.name.lower
    participant self_model_catalog_items as self.model_catalog.items
    participant patterns_keys as patterns.keys
    participant self_task_models_get as self.task_models.get
    participant self_available_categories_get as self.available_categories.get
    participant output_split as output.split
    participant response_json as response.json
    participant model_info_get as model_info.get
    participant self_models_get as self.models.get
    participant self__MODEL_TEMPERATURES_get as self._MODEL_TEMPERATURES.get
    participant model_map_get as model_map.get
    participant required_keys_issubset as required_keys.issubset
    participant result_keys as result.keys
    participant blueprint_get as blueprint.get
    participant phase_get as phase.get
    participant agent_get as agent.get
    participant toon_final_get as toon_final.get
    participant k_startswith as k.startswith
    participant full_selection_get as full_selection.get
    participant pg_module_PromptGenerator as pg_module.PromptGenerator
    participant generator_synthesize_with_flow_data as generator.synthesize_with_flow_data
    participant plan_exec_get as plan_exec.get
    participant synthesis_result_get as synthesis_result.get
    participant synthesis_get as synthesis.get
    participant start_flag_exists as start_flag.exists
    participant start_flag_write_text as start_flag.write_text
    participant key_endswith as key.endswith
    participant stats_get as stats.get
    participant StateGraph
    participant graph_add_node as graph.add_node
    participant graph_add_edge as graph.add_edge
    participant graph_add_conditional_edges as graph.add_conditional_edges
    participant sys_stderr_isatty as sys.stderr.isatty
    participant graph_compile as graph.compile
    participant getenv
    participant time_monotonic as time.monotonic
    participant ParallelExplorer
    participant ThreadPoolExecutor
    participant as_completed
    participant pool_submit as pool.submit
    participant fut_result as fut.result
    participant ParallelTaskExecutor
    participant results_map_get as results_map.get
    participant results_map_values as results_map.values
    participant files_intersection as files.intersection
    participant b_files_update as b_files.update
    participant ConcurrentSkillDownloader
    participant spec_get as spec.get
    participant domain_dir_iterdir as domain_dir.iterdir
    participant domain_dir_is_dir as domain_dir.is_dir
    participant _CacheEntry
    participant patterns
    participant raw_encode as raw.encode
    participant kwargs_items as kwargs.items
    participant OrderedDict
    participant cache_move_to_end as cache.move_to_end
    participant cache_popitem as cache.popitem
    participant t_is_alive as t.is_alive
    participant _logger_debug as _logger.debug
    participant _logger_error as _logger.error
    participant existing_metadata_update as existing.metadata.update
    participant cls__registry_items as cls._registry.items
    participant skills_items as skills.items
    participant fnmatch_fnmatch as fnmatch.fnmatch
    participant cls__registry_keys as cls._registry.keys
    participant cls__registry_values as cls._registry.values
    participant self_steps_items as self.steps.items
    participant self_steps_values as self.steps.values
    participant self_steps_keys as self.steps.keys
    participant self_benchmark_dir_mkdir as self.benchmark_dir.mkdir
    participant bdir_is_dir as bdir.is_dir
    participant bdir_glob as bdir.glob
    participant risks_get as risks.get
    participant requirement_split as requirement.split
    participant generate_plan_fn
    participant _RISK_BANDS_items as _RISK_BANDS.items
    participant PolicyNodeAdapter
    participant relative_path_exists as relative_path.exists
    participant cwd_path_exists as cwd_path.exists
    participant self__script_full_path_exists as self._script_full_path.exists
    participant self_script_path_is_absolute as self.script_path.is_absolute
    participant self_script_path_exists as self.script_path.exists
    participant self_input_mapping_items as self.input_mapping.items
    participant output_strip as output.strip
    participant self_output_mapping_items as self.output_mapping.items
    participant adapter
    participant StepRecord
    participant STEP_LABELS_get as STEP_LABELS.get
    participant ProgressDisplay
    participant self__steps_values as self._steps.values
    participant self__steps_items as self._steps.items
    participant self__history_get as self._history.get
    participant STATUS_ICONS_get as STATUS_ICONS.get
    participant self_stream_write as self.stream.write
    participant self_stream_flush as self.stream.flush
    participant progress_display
    participant self__vf_get as self._vf.get
    participant STEP_THRESHOLDS_get as STEP_THRESHOLDS.get
    participant best_get as best.get
    participant payload_get as payload.get
    participant context_get as context.get
    participant filter
    participant cb
    participant _current_state_get as _current_state.get
    participant signal_signal as signal.signal
    participant threading_current_thread as threading.current_thread
    participant threading_main_thread as threading.main_thread
    participant self_error_logger_log_decision as self.error_logger.log_decision
    participant CriteriaResult
    participant all_issues_extend as all_issues.extend
    participant all_warnings_extend as all_warnings.extend
    participant review_criteria
    participant n_startswith as n.startswith
    participant content_lstrip as content.lstrip
    participant pr_body_strip as pr_body.strip
    participant p_is_absolute as p.is_absolute
    participant fn_match_group as fn_match.group
    participant run_pipeline
    participant cp_get as cp.get
    participant run_parser_add_argument as run_parser.add_argument
    participant resume_parser_add_argument as resume_parser.add_argument
    participant list_parser_add_argument as list_parser.add_argument
    participant latest_read_text as latest.read_text
    participant self_session_dir_glob as self.session_dir.glob
    participant logs_file_exists as logs_file.exists
    participant logs_file_read_text as logs_file.read_text
    participant self_session_dir_stat as self.session_dir.stat
    participant old_file_unlink as old_file.unlink
    participant self_skills_dir_glob as self.skills_dir.glob
    participant self_skills_dir_iterdir as self.skills_dir.iterdir
    participant self_skills_dir_exists as self.skills_dir.exists
    participant skill_dir_iterdir as skill_dir.iterdir
    participant sub_skill_file_exists as sub_skill_file.exists
    participant sub_dir_is_dir as sub_dir.is_dir
    participant sub_skill_file_read_text as sub_skill_file.read_text
    participant frontmatter_split as frontmatter.split
    participant content_startswith as content.startswith
    participant value_strip as value.strip
    participant self_agents_dir_iterdir as self.agents_dir.iterdir
    participant self_agents_dir_exists as self.agents_dir.exists
    participant agents_items as agents.items
    participant skill_agent_loader
    participant github_raw_base_rstrip as github_raw_base.rstrip
    participant dep_result_get as dep_result.get
    participant version_requirements_get as version_requirements.get
    participant skill_contents_items as skill_contents.items
    participant raw_decode as raw.decode
    participant self__cache_get as self._cache.get
    participant self_skills_root_exists as self.skills_root.exists
    participant self_skills_root_glob as self.skills_root.glob
    participant found_add as found.add
    participant skill_name_encode as skill_name.encode
    participant patterns_file_exists as patterns_file.exists
    participant patterns_file_read_text as patterns_file.read_text
    participant vf_get as vf.get
    participant patterns_data_get as patterns_data.get
    participant skill_keywords_add as skill_keywords.add
    participant skill_name_replace as skill_name.replace
    participant skill1_get as skill1.get
    participant skill2_get as skill2.get
    participant name2_lower as name2.lower
    participant name1_lower as name1.lower
    participant scored_sort as scored.sort
    participant STANDARDS_INTEGRATION_POINTS_get as STANDARDS_INTEGRATION_POINTS.get
    participant hook_dispatch_get as hook_dispatch.get
    participant hook_fn
    participant standards_get as standards.get
    participant project_type_upper as project_type.upper
    participant spring_patterns_get as spring_patterns.get
    participant tool_rules_get as tool_rules.get
    participant skill_name_lower as skill_name.lower
    participant StandardsSchema
    participant self_FIELD_TYPES_items as self.FIELD_TYPES.items
    participant standard_dict_get as standard_dict.get
    participant yaml_str_splitlines as yaml_str.splitlines
    participant line_rstrip as line.rstrip
    participant yaml_safe_load as yaml.safe_load
    participant top_match_group as top_match.group
    participant nested_match_group as nested_match.group
    participant raw_val_lower as raw_val.lower
    participant raw_val_strip as raw_val.strip
    participant errors_extend as errors.extend
    participant standard_update as standard.update
    participant app_py_exists as app_py.exists
    participant app_py_read_text as app_py.read_text
    participant req_file_read_text as req_file.read_text
    participant pom_xml_exists as pom_xml.exists
    participant gradle_path_exists as gradle_path.exists
    participant pom_xml_read_text as pom_xml.read_text
    participant gradle_path_read_text as gradle_path.read_text
    participant deps_update as deps.update
    participant package_json_read_text as package_json.read_text
    participant k_lower as k.lower
    participant deps_keys as deps.keys
    participant standards_dir_glob as standards_dir.glob
    participant standards_dir_exists as standards_dir.exists
    participant candidate_exists as candidate.exists
    participant candidate_read_text as candidate.read_text
    participant merged_rules_keys as merged_rules.keys
    participant rules1_get as rules1.get
    participant rules2_get as rules2.get
    participant sub1_keys as sub1.keys
    participant sub2_keys as sub2.keys
    participant override_items as override.items
    participant step_logger
    participant step_name_replace as step_name.replace
    participant result_items as result.items
    participant name_strip as name.strip
    participant selection_get as selection.get
    participant prompt_strip as prompt.strip
    participant referenced_update as referenced.update
    participant graph_keys as graph.keys
    participant graph_items as graph.items
    participant deque
    participant queue_popleft as queue.popleft
    participant path_stack_pop as path_stack.pop
    participant path_stack_index as path_stack.index
    participant _EFFORT_TOKEN_COST_get as _EFFORT_TOKEN_COST.get
    participant StepTimeout
    participant _StepResult
    participant thread_start as thread.start
    participant thread_join as thread.join
    participant result_setdefault as result.setdefault
    participant thread_is_alive as thread.is_alive
    participant STEP_TIMEOUTS_get as STEP_TIMEOUTS.get
    participant BudgetExceededError
    participant self_ALLOCATIONS_values as self.ALLOCATIONS.values
    participant self_usage_values as self.usage.values
    participant self_ALLOCATIONS_get as self.ALLOCATIONS.get
    participant self_usage_get as self.usage.get
    participant s_isdigit as s.isdigit
    participant self_ALLOCATIONS_items as self.ALLOCATIONS.items
    participant self_usage_items as self.usage.items
    participant orjson_dumps as orjson.dumps
    participant toon_model_dump as toon.model_dump
    participant orjson_loads as orjson.loads
    participant model
    participant ui_components
    participant shutil_get_terminal_size as shutil.get_terminal_size
    participant PipelineUI
    participant self__progress_start as self._progress.start
    participant all_explanations_get as all_explanations.get
    participant self__step_errors_setdefault as self._step_errors.setdefault
    participant self__progress_get_summary as self._progress.get_summary
    participant self__step_errors_values as self._step_errors.values
    participant item_name_endswith as item.name.endswith
    participant ast_dump as ast.dump
    participant attr_name_startswith as attr_name.startswith
    participant all_classes_extend as all_classes.extend
    participant deps_add as deps.add
    participant imp_split as imp.split
    participant callee_raw_split as callee_raw.split
    participant callee_name_split as callee_name.split
    participant caller_name_endswith as caller_name.endswith
    participant cg_classes_items as cg.classes.items
    participant cls_node_get as cls_node.get
    participant attrs_by_name_get as attrs_by_name.get
    participant cg_methods_get as cg.methods.get
    participant ac_get as ac.get
    participant from_fqn_split as from_fqn.split
    participant to_fqn_split as to_fqn.split
    participant callee_fqn_split as callee_fqn.split
    participant caller_name_split as caller_name.split
    participant scope_path_is_file as scope_path.is_file
    participant attr_get as attr.get
    participant dep_graph_keys as dep_graph.keys
    participant connected_add as connected.add
    participant connected_update as connected.update
    participant groups_items as groups.items
    participant group_replace as group.replace
    participant call_chains_extend as call_chains.extend
    participant _participant_name
    participant _method_name
    participant after_split as after.split
    participant name_replace as name.replace
    participant srs_path_read_text as srs_path.read_text
    participant readme_path_read_text as readme_path.read_text
    participant self_project_root_glob as self.project_root.glob
    participant self_project_root_iterdir as self.project_root.iterdir
    participant call_graph_methods_items as call_graph.methods.items
    participant class_methods_items as class_methods.items
    participant all_method_data_extend as all_method_data.extend
    participant all_callee_fqns_add as all_callee_fqns.add
    participant fqn_to_nid_get as fqn_to_nid.get
    participant _safe_id
    participant cls_name_replace as cls_name.replace
    participant mname_startswith as mname.startswith
    participant parent_cls_split as parent_cls.split
    participant class_methods_setdefault as class_methods.setdefault
    participant p_split as p.split
    participant fqn_replace as fqn.replace
    participant method
    participant cg_classes_values as cg.classes.values
    participant out_path_write_text as out_path.write_text
    participant syntax_strip as syntax.strip
    participant diagram_text_encode as diagram_text.encode
    participant output_path_parent_mkdir as output_path.parent.mkdir
    participant output_path_write_bytes as output_path.write_bytes
    participant s_find as s.find
    participant text_startswith as text.startswith
    participant text_endswith as text.endswith
    participant pre_lower as pre.lower
    participant _VERSION_PATTERN_match as _VERSION_PATTERN.match
    participant version_str_strip as version_str.strip
    participant self__PRE_RANK_get as self._PRE_RANK.get
    participant req_split as req.split
    participant specifier_strip as specifier.strip
    participant specifier_startswith as specifier.startswith
    participant version_reqs_items as version_reqs.items
    participant skill_versions_get as skill_versions.get
    participant skill_content_lower as skill_content.lower
    participant requirements_items as requirements.items
    participant script_name_replace as script_name.replace
    participant _time_mod_time as _time_mod.time
    participant session_path_mkdir as session_path.mkdir
    participant _prune_result_get as _prune_result.get
    participant complexity_script_exists as complexity_script.exists
    participant project_root_glob as project_root.glob
    participant cache_stats_get as cache_stats.get
    participant project_root_exists as project_root.exists
    participant original_context_get as original_context.get
    participant toon_object_get as toon_object.get
    participant toons_dumps as toons.dumps
    participant updates_update as updates.update
    participant cleanup_update as cleanup.update
    participant toon_keys as toon.keys
    participant _usage_get as _usage.get
    participant level_path_glob as level_path.glob
    participant policy_file_read_text as policy_file.read_text
    participant script_result_get as script_result.get
    participant policies_result_get as policies_result.get
    participant policies_dir_glob as policies_dir.glob
    participant rules_keys as rules.keys
    participant updates_get as updates.get
    participant analysis_result_get as analysis_result.get
    participant breakdown_result_get as breakdown_result.get
    participant doc_info_get as doc_info.get
    participant ah_result_get as ah_result.get
    participant state_keys as state.keys
    participant llm_response_strip as llm_response.strip
    participant cleaned_startswith as cleaned.startswith
    participant parsed_get as parsed.get
    participant cleaned_split as cleaned.split
    participant cleaned_lstrip as cleaned.lstrip
    participant refined_toon_get as refined_toon.get
    participant plan_details_get as plan_details.get
    participant task_files_update as task_files.update
    participant all_skills_get as all_skills.get
    participant all_agents_get as all_agents.get
    participant all_skills_keys as all_skills.keys
    participant all_agents_keys as all_agents.keys
    participant deepseek_mcp_reasoning_to_dict as deepseek_mcp_reasoning.to_dict
    participant boosted_sort as boosted.sort
    participant resolution_get as resolution.get
    participant debug
    participant _logging_getLogger as _logging.getLogger
    participant skill_path_lower_exists as skill_path_lower.exists
    participant agent_path_exists as agent_path.exists
    participant skill_path_upper_exists as skill_path_upper.exists
    participant re_compile as re.compile
    participant explicit_re_finditer as explicit_re.finditer
    participant codeblock_re_finditer as codeblock_re.finditer
    participant path_re_finditer as path_re.finditer
    participant f_replace as f.replace
    participant m_group as m.group
    participant f_startswith as f.startswith
    participant _json_loads as _json.loads
    participant srs_file_exists as srs_file.exists
    participant srs_file_read_text as srs_file.read_text
    participant islower
    participant user_message_strip as user_message.strip
    participant llm_title_strip as llm_title.strip
    participant user_msg_strip as user_msg.strip
    participant msg_lower_startswith as msg_lower.startswith
    participant label_lower as label.lower
    participant issue_id_isdigit as issue_id.isdigit
    participant user_message_file_exists as user_message_file.exists
    participant user_message_file_read_text as user_message_file.read_text
    participant changes_summary_get as changes_summary.get
    participant pr_id_isdigit as pr_id.isdigit
    participant cleanup_get as cleanup.get
    participant doc_file_write_text as doc_file.write_text
    participant voice_script_exists as voice_script.exists
    participant summary_get as summary.get
    participant k_endswith as k.endswith
    participant step_label_lower as step_label.lower
    participant _timeouts_get as _timeouts.get
    participant _StepTimeout
    participant tw_run as tw.run
    participant step_fn
    participant rag_result_get as rag_result.get
    participant metrics_record_step as metrics.record_step
    participant mem_path_write_text as mem_path.write_text
    participant _json_dumps as _json.dumps
    participant memory_file_is_file as memory_file.is_file
    participant memory_file_read_text as memory_file.read_text
    participant workflow_memory_get as workflow_memory.get
    participant impact_data_get as impact_data.get
    participant sleep
    participant call_context_get as call_context.get
    participant impact_review_get as impact_review.get
    participant bc_get as bc.get
    participant st_get as st.get
    participant sys_stdout_reconfigure as sys.stdout.reconfigure
    participant sys_stderr_reconfigure as sys.stderr.reconfigure
    participant io_TextIOWrapper as io.TextIOWrapper
    participant updates_keys as updates.keys
    participant py_file_read_bytes as py_file.read_bytes
    participant content_decode as content.decode
    participant input
    participant line_replace as line.replace
    participant files_data_values as files_data.values
    participant sessions_root_exists as sessions_root.exists
    participant sessions_root_iterdir as sessions_root.iterdir
    participant directory_iterdir as directory.iterdir
    participant directory_exists as directory.exists
    participant item_is_file as item.is_file
    participant item_suffix_lower as item.suffix.lower
    participant item_read_text as item.read_text
    participant recent_reverse as recent.reverse
    participant zone_labels_get as zone_labels.get
    participant is_dir
    participant detected_add as detected.add
    participant file_cache_get as file_cache.get
    participant fpath_stat as fpath.stat
    participant ext_lower as ext.lower
    participant root_iterdir as root.iterdir
    participant rule_lookup_get as rule_lookup.get
    participant category_map_setdefault as category_map.setdefault
    participant projects_root_exists as projects_root.exists
    participant projects_root_iterdir as projects_root.iterdir
    participant category_upper as category.upper
    participant all_results_items as all_results.items
    participant Counter
    participant TASK_TYPE_KEYWORDS_items as TASK_TYPE_KEYWORDS.items
    participant agent_lower as agent.lower
    participant keyword_lower as keyword.lower
    participant seen_task_types_this_session_add as seen_task_types_this_session.add
    participant item_iterdir as item.iterdir
    participant subitem_is_file as subitem.is_file
    participant subitem_suffix_lower as subitem.suffix.lower
    participant counts_most_common as counts.most_common
    participant preferences_file_exists as preferences_file.exists
    participant preferences_file_read_text as preferences_file.read_text
    participant preferences_file_parent_mkdir as preferences_file.parent.mkdir
    participant preferences_file_write_text as preferences_file.write_text
    participant existing_items_items as existing_items.items
    participant new_items_items as new_items.items
    participant item_copy as item.copy
    participant new_get as new.get
    participant combined_values as combined.values
    participant _print_category
    participant all_session_files_sort as all_session_files.sort
    participant by_month_items as by_month.items
    participant by_month_setdefault as by_month.setdefault
    participant archive_path_exists as archive_path.exists
    participant tarfile_open as tarfile.open
    participant tar_add as tar.add
    participant archive_path_with_suffix as archive_path.with_suffix
    participant tmp_path_exists as tmp_path.exists
    participant old_tar_getmembers as old_tar.getmembers
    participant tmp_path_unlink as tmp_path.unlink
    participant old_tar_extractfile as old_tar.extractfile
    participant new_tar_add as new_tar.add
    participant new_tar_addfile as new_tar.addfile
    participant LOG_DIR_mkdir as LOG_DIR.mkdir
    participant all_files_sort as all_files.sort
    participant archive_base_exists as archive_base.exists
    participant archive_base_rglob as archive_base.rglob
    participant tar_file_stat as tar_file.stat
    participant tar_getmembers as tar.getmembers
    participant StandardsLoader
    participant self_policies_dir_exists as self.policies_dir.exists
    participant self_policies_dir_rglob as self.policies_dir.rglob
    participant self_standards_values as self.standards.values
    participant LANG_EXTENSIONS_items as LANG_EXTENSIONS.items
    participant extensions_extend as extensions.extend
    participant lang_lower as lang.lower
    participant os_walk as os.walk
    participant alias_name_split as alias.name.split
    participant node_module_split as node.module.split
    participant pattern_finditer as pattern.finditer
    participant iface_strip as iface.strip
    participant block_match_group as block_match.group
    participant file_path_suffix_lower as file_path.suffix.lower
    participant lizard_analyze_file as lizard.analyze_file
    participant nx_DiGraph as nx.DiGraph
    participant fpath_relative_to as fpath.relative_to
    participant graph_number_of_nodes as graph.number_of_nodes
    participant graph_number_of_edges as graph.number_of_edges
    participant nx_density as nx.density
    participant nx_pagerank as nx.pagerank
    participant nx_dag_longest_path_length as nx.dag_longest_path_length
    participant nx_number_weakly_connected_components as nx.number_weakly_connected_components
    participant graph_to_undirected as graph.to_undirected
    participant nx_average_clustering as nx.average_clustering
    participant nx_betweenness_centrality as nx.betweenness_centrality
    participant graph_out_degree as graph.out_degree
    participant betweenness_values as betweenness.values
    participant pagerank_values as pagerank.values
    participant centrality_items as centrality.items
    participant CodeGraphAnalyzer
    participant self_project_root_exists as self.project_root.exists
    participant self_cyclomatic_get as self.cyclomatic.get
    participant local_cache_dir_mkdir as local_cache_dir.mkdir
    participant local_cache_file_write_text as local_cache_file.write_text
    participant self_metrics_get as self.metrics.get
    participant LOG_FILE_parent_mkdir as LOG_FILE.parent.mkdir
    participant MEMORY_DIR_mkdir as MEMORY_DIR.mkdir
    participant OllamaTaskAnalyzer
    participant analysis_get as analysis.get
    participant llm_response_index as llm_response.index
    participant llm_response_rindex as llm_response.rindex
    participant prompt_generator as prompt-generator
    participant arg_replace as arg.replace
    participant TaskAutoAnalyzer
    participant entities_extend as entities.extend
    participant message_lower as message.lower
    participant tasks_extend as tasks.extend
    participant entity_capitalize as entity.capitalize
    participant task_map_items as task_map.items
    participant self_logs_path_mkdir as self.logs_path.mkdir
    participant task_auto_analyzer as task-auto-analyzer
    participant _llm_call
    participant AutoPlanModeSuggester
    participant complexity_analysis_get as complexity_analysis.get
    participant complexity_analysis_copy as complexity_analysis.copy
    participant structured_prompt_get as structured_prompt.get
    participant complexity_get as complexity.get
    participant auto_plan_mode_suggester as auto-plan-mode-suggester
    participant llm_result_get as llm_result.get
    participant AutoSkillAgentSelector
    participant _skills_dir_exists as _skills_dir.exists
    participant _agents_dir_exists as _agents_dir.exists
    participant _skills_dir_iterdir as _skills_dir.iterdir
    participant _agents_dir_iterdir as _agents_dir.iterdir
    participant prompt_get as prompt.get
    participant tech_map_items as tech_map.items
    participant tech_lower_startswith as tech_lower.startswith
    participant tech_lower_endswith as tech_lower.endswith
    participant tech_lower as tech.lower
    participant key_title as key.title
    participant auto_skill_agent_selector as auto-skill-agent-selector
    participant skill_md_exists as skill_md.exists
    participant skill_md_read_text as skill_md.read_text
    participant agent_md_exists as agent_md.exists
    participant agent_md_read_text as agent_md.read_text
    participant project_type_strip as project_type.strip
    participant yaml_dump as yaml.dump
    participant available_skills_keys as available_skills.keys
    participant proj_type_lower as proj_type.lower
    participant available_skills_get as available_skills.get
    participant relevant_agents_items as relevant_agents.items
    participant _matches_project
    participant available_agents_items as available_agents.items
    participant a_strip as a.strip
    participant _is_language_specific
    3_level_flow->>strftime: strftime()
    3_level_flow->>upper: upper()
    3_level_flow->>datetime_now: datetime.now()
    3_level_flow->>uuid_uuid4: uuid.uuid4()
    3_level_flow->>strip: strip()
    3_level_flow->>str: str()
    3_level_flow->>AtomicJsonStore: exists()
    3_level_flow->>Path_cwd: Path.cwd()
    3_level_flow->>os_environ_get: os.environ.get()
    3_level_flow->>Path: Path()
    3_level_flow->>sys_stdin_isatty: sys.stdin.isatty()
    3_level_flow->>sys_stdin_read: sys.stdin.read()
    3_level_flow->>json_loads: json.loads()
    3_level_flow->>event_get: event.get()
    3_level_flow->>orchestrator: create_initial_state()
    3_level_flow->>orchestrator: create_flow_graph()
    3_level_flow->>checkpointer: get_invoke_config()
    3_level_flow->>HybridInferenceManager: invoke()
    3_level_flow->>RuntimeError: RuntimeError()
    3_level_flow->>3_level_flow: _generate_session_id()
    3_level_flow->>3_level_flow: _get_project_root()
    3_level_flow->>3_level_flow: _capture_user_message()
    3_level_flow->>print: print()
    3_level_flow->>list: list()
    3_level_flow->>initial_state_get: initial_state.get()
    3_level_flow->>result_get: result.get()
    3_level_flow->>len: len()
    3_level_flow->>initial_state_keys: initial_state.keys()
    3_level_flow->>memory_dir_exists: memory_dir.exists()
    3_level_flow->>policies_dir_exists: policies_dir.exists()
    3_level_flow->>Path_home: Path.home()
    3_level_flow->>3_level_flow: _validate_environment()
    3_level_flow->>threading_Event: threading.Event()
    3_level_flow->>threading_Thread: threading.Thread()
    3_level_flow->>engine_thread_start: engine_thread.start()
    3_level_flow->>_timeout_event_wait: _timeout_event.wait()
    3_level_flow->>flow_trace_converter: write_flow_trace_json()
    3_level_flow->>flow_trace_converter: print_flow_checkpoint()
    3_level_flow->>sys_exit: sys.exit()
    3_level_flow->>arg_startswith: arg.startswith()
```

## Notes

- Generated from: .
- Scope: Full project
