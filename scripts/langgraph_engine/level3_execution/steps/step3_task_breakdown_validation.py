"""
REMOVED (v1.15.0) -- Step 3: Task Breakdown Validation

This module was removed as dead code. Removed in v1.13.0 when Steps 1, 3, 4,
5, 6, 7 were collapsed into the Step 0 orchestration template call.

If you see this file being imported, that import is stale and should be removed.
"""

raise ImportError(
    "step3_task_breakdown_validation is removed (v1.13.0). "
    "Task breakdown is handled by the orchestration template in Step 0."
)
